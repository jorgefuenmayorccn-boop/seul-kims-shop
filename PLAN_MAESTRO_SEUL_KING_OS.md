# SEUL KING OS v1.0 — Plan Maestro de Ejecución (v1.0, 31-ago-2026)

**Este es el plan único de ejecución para dejar SEUL KING OS listo para entrega. No improvisar fuera de él.**

Repo: `/Users/vertice/vertice_productions/seul-kims-shop` (monorepo pnpm — `apps/web`, `apps/cerebro`, `apps/pos`, `apps/repartidor`, `packages/api`, `packages/db`)

---

## 0. Por qué existe este plan

El 31-ago-2026 se reportó y arregló un bug de login (cookie de sesión `__Host-` que no cruzaba subdominios — commits `30d193a`, `1efb693`, `618429f`). Al verificar el sistema completo en navegador real después del fix, aparecieron **capturas de pantalla mostrando que casi ninguna pantalla del panel carga datos** (Comandas, Despacho, Turnos, Ajustes, Usuarios, abrir turno en POS). Al auditar `packages/api/src/server.ts` contra cada `fetch()` de las 4 apps, se confirmó que **el problema no es un bug — es que la mayoría del backend nunca se construyó**.

**El dato más importante de esta auditoría:** el schema de base de datos (`packages/db/src/schema/*.ts`) **ya modela casi todo el negocio** — `products`, `inventory`, `orders`, `customers`, `delivery`, `shifts`, `till-sessions`, `b2b-quotes`, `payments`, `dte-events`, `returns`, `promotions`, `loyalty`, `faq`, `hero-banners`, `customer-auth`, `pos-void-events`. Esto **no es un proyecto que hay que diseñar desde cero** — es un proyecto donde el modelo de datos está listo y **faltan las rutas de API que lo exponen** más el cableado del frontend. Eso cambia radicalmente el esfuerzo real frente a construir todo desde cero.

---

## 1. Dónde está todo

| Archivo | Para qué |
|---|---|
| `PLAN_MAESTRO_SEUL_KING_OS.md` (este archivo) | Roadmap completo, bloqueadores P0 con archivo:línea, gap de endpoints completo |
| `packages/api/src/server.ts` | Backend único (Hono + Node, corre en Railway, NO en Cloudflare Workers pese a lo que decía `wrangler.toml`) |
| `packages/db/src/schema/*.ts` | Modelo de datos completo — 21 tablas, ya construido |
| `SEUL_KING_OS_v1.0_MANUAL_CLIENTE.md` / `SEUL_KING_OS_v1_Manual.html` (Desktop) | Manuales ya actualizados con el estado real post-fix de login — **hay que volver a actualizarlos cuando cierre cada fase** |
| `~/.claude/projects/-Users-vertice-vertice-productions/sessions/SEUL_SESSION_NN.md` | Memoria por sesión (crear una por cada sesión ejecutada, mismo formato que las `SESSION_NN.md` del CRM) |

## 2. Protocolo de sesión (obligatorio — mismo que VÉRTICE CRM)

1. **Al empezar:** `git status` + `git log -5` primero — este repo ha tenido **trabajo concurrente sin commitear** (se encontró `packages/api/src/server.ts` modificado en vivo el 31-ago, agregando `/api/auth/users` CRUD, sin relación con esta sesión). Nunca asumir que el árbol de trabajo está limpio.
2. Leer la fila de la fase/sesión en la sección 5 de este documento antes de tocar código.
3. **Al cerrar:** commitear todo lo trabajado (nunca dejar diffs sueltos — el hallazgo del punto 1 es exactamente el riesgo que se evita commiteando), actualizar la tabla de estado de módulos (sección 6), y escribir `SEUL_SESSION_NN.md` con `## ✅ Completado`, `## ❌ Bloqueadores`, `## Memory para próxima sesión`.
4. Sin commit + sin memoria escrita, la sesión **no está cerrada**.

## 3. Roadmap en una línea

Fase 0 Seguridad y Estabilización (S01-02, 16h, 🔴 BLOQUEADORA) → Fase 1 Núcleo Operación Interna — Panel Admin + POS (S03-06, 64h) → Fase 2 Repartidor + Tiempo Real (S07-08, 28h) → Fase 3 Portal B2B + Tienda Web Cliente (S09-11, 48h) → Fase 4 Cumplimiento Legal + Analítica (S12-14, 44h) → Fase 5 Diseño Premium + Hardening + Entrega (S15-17, 40h). **Total ≈ 240h / 17 sesiones / 6 fases.**

## 4. Los bloqueadores P0 (verificados en código, 31-ago-2026)

1. **`JWT_SECRET` tiene un fallback inseguro hardcodeado** (`packages/api/src/db.ts:10` — `process.env.JWT_SECRET || 'seul-king-os-secret-dev'`). Si la variable de entorno faltara en Railway en cualquier momento (redeploy, cambio de servicio), el sistema firmaría sesiones válidas con un secreto público que está en el repo de GitHub. `DATABASE_URL` ya hace `throw` si falta en producción (`db.ts:22-24`) — `JWT_SECRET` debe hacer exactamente lo mismo. Fix: 15 min, cero excusa para no hacerlo en S01.
2. **`requireAuthMiddleware` no valida JWT, solo API Keys** (`packages/api/src/middleware/auth.middleware.ts:36` — `// TODO: Implement JWT validation`). Cada endpoint nuevo (incluido el `/api/auth/users` que se está agregando sin commitear ahora mismo) reimplementa su propio parseo de cookie/Bearer copiando el patrón de `handleGetMe` en vez de usar un middleware central. Es deuda técnica que ya se está replicando — congelarla en S01 con un `requireSession()` único antes de que crezca más.
3. **Rate limiter incompleto** (`packages/api/src/middleware/auth.middleware.ts:75` — `// TODO: Implement rate limiter (use KV store)`). Hoy solo `/api/auth/login` tiene rate limiting (tabla `login_attempts`, 5 intentos/15 min). Ningún otro endpoint de escritura lo tiene — `/api/orders`, `/api/b2b/quotes`, `/api/auth/register` están abiertos a abuso.
4. **Sin RBAC real en el frontend ni en el backend.** El `Sidebar` de cerebro (`apps/cerebro/src/components/layout/sidebar.tsx`) muestra el mismo menú completo a `owner`, `admin`, `staff` y `viewer` — no hay diferenciación. En el backend, casi ningún endpoint valida `role` más allá de "¿hay sesión?". El manual de cliente ya se corrigió para no prometer esto — pero es la brecha de seguridad/UX más visible para el dueño y hay que cerrarla antes de dar acceso a más personal.
5. **Gap de endpoints — ver tabla completa en sección 7.** No es un blocker puntual, es sistémico: de ~45 rutas que las 4 apps llaman, **~20 existen** (incluyendo lo que se está agregando sin commitear ahora) y **~25 no existen**, con algunas rutas existentes bajo un path distinto al que el frontend llama (`/api/deliveries/assign` en el backend vs `/api/delivery/assignments/:id/assign` en Despacho — mismatch de nombre, no solo ausencia).
6. **Cero endpoint de facturación electrónica (SII/DTE)** pese a que `packages/db/src/schema/dte-events.ts` ya existe y el manual del cliente promete cumplimiento legal chileno. Operar sin boleta electrónica es el riesgo #1 marcado como 🔴 crítico en el plan de negocio original (`SEOUL_KIMS_OS_v1.0_Plan_de_Accion.md`, sección 13, riesgo #1: "multas y clausura").
7. **Trabajo sin commitear en este momento** — `packages/api/src/server.ts` tiene un diff de 132 líneas sin commit (CRUD de `/api/auth/users`) al momento de escribir este plan. **Antes de tocar nada de la sección 5, alguien tiene que revisar y commitear (o descartar deliberadamente) ese diff.** No es tarea de este plan resolverlo por adivinanza — es lo primero que se hace en S01.

## 5. Reglas duras

- Los gates go/no-go **no se negocian**. Si un endpoint nuevo no pasa su prueba end-to-end en navegador real (no solo curl), la sesión no cierra.
- **Toda ruta nueva usa el modelo de `packages/db/src/schema/*.ts` que ya existe** — antes de escribir un endpoint, leer el schema de la tabla correspondiente. No inventar columnas nuevas sin revisar primero si ya están modeladas.
- **Cero regresión en lo que ya funciona:** login (las 4 apps), `/api/orders` (crear), `/api/b2b/quotes` (crear/aceptar/rechazar), `/api/deliveries/:id/status` y `/:id/photo`, `/api/admin/api-keys`. Cada sesión corre un smoke test de estos antes de cerrar.
- Railway auto-deploya el API en cada push a `main`; Vercel auto-deploya las 4 apps. No existe "commit sin deploy" — probar en producción real después de cada push, no asumir.
- Cada endpoint nuevo que toca datos de otro usuario (no el propio) valida `role` server-side, no solo "hay sesión". Ver bloqueador P0 #4.
- Cada fase que agrega una pantalla nueva o cambia una existente actualiza `SEUL_KING_OS_v1.0_MANUAL_CLIENTE.md` en el mismo commit — el manual no puede volver a quedar desincronizado del sistema real.

## 6. Estado de los módulos (31-ago-2026)

| Módulo | App(s) | Backend | Estado |
|---|---|---|---|
| Auth staff (login/logout/me/change-password) | cerebro, pos, repartidor | ✅ completo | Verificado en navegador real las 3 apps |
| Usuarios (listar/crear/editar/desactivar) | cerebro | ✅ completo + RBAC | Commiteado (S01), RBAC server-side owner-only en editar/desactivar/crear (S02), verificado end-to-end contra producción |
| Dashboard (stats, alertas) | cerebro | ✅ completo | S04 (1-sep-2026): `GET /api/dashboard/stats`, `GET /api/dashboard/alerts`. Roles owner/admin/viewer (staff no tiene Dashboard en la matriz de 6.1). Banner "API no disponible" resuelto — verificado con curl contra producción (Neon real): 9 pedidos activos, 1 lote por vencer real (Milkis Lotte 250ml). `stockCritico` usa umbral fijo de 5 u. (no existe `products.min_stock`); `b2bPendientes` usa cotizaciones sent/viewed (no existe wallet B2B todavía, Fase 3) — ver `SEUL_SESSION_04.md` |
| Productos + categorías | cerebro, web, pos | ✅ completo | S03 (1-sep-2026): `GET /api/products`, `/api/products/meta/categories`, `/api/products/barcode/:code`, `/api/products/id/:id`. Verificado con curl contra producción con datos reales (81 productos, 20 categorías) — ver `SEUL_SESSION_03.md` |
| Inventario | cerebro | ✅ completo | S03 (1-sep-2026): `GET /api/inventory` con semáforo de vencimiento (mismos umbrales que `BadgeExpiry`) y filtros category/expiry/cold_chain/baes. Verificado con curl contra producción (65 lotes reales) — ver `SEUL_SESSION_03.md` |
| Comandas (pedidos, listar/gestionar) | cerebro | ✅ completo | S04 (1-sep-2026) construyó `GET /api/orders` y `GET /api/orders/comandas`; el mismatch PATCH/CORS quedó resuelto en commit `6b3d7fa` (previo a esta sesión). **Auditoría de cierre Fase 1 (1-sep-2026) encontró un bug adicional que ni el curl de S04 ni el fix de CORS detectaron:** `comandas/page.tsx` hacía `fetch()` sin `credentials: 'include'` — como `cmr.seoulshop.cl` y `api.seoulshop.cl` son orígenes distintos, el navegador nunca mandaba la cookie `seul_session`, así que el endpoint (que sí funciona) respondía 401 en producción real y la página crasheaba con "Cannot read properties of undefined (reading 'length')". `curl -b cookies.txt` no puede detectar este tipo de bug porque adjunta la cookie a mano. Fix de 2 líneas (agregar `credentials: 'include'` a ambos `fetch()`) — commit `f32a532`. Verificado con Playwright real contra producción tras el fix: carga 200, mover tarjeta (Preparar → Lista → ↩) dispara `PATCH /api/orders/:id/status` → 200, confirmado visualmente. |
| Despacho (asignar entregas) | cerebro, pos | ✅ completo | S05 (1-sep-2026): el mismatch de paths ya estaba resuelto (commit `6084177`, S02) — `GET /api/delivery/assignments` y `PUT /api/delivery/assignments/:id/assign` (singular) existen y son lo único que las 4 apps llaman de verdad (confirmado con grep, cero `fetch` a `/api/deliveries/*` plural en ninguna app). **Decisión (S05): mantener ambos prefijos, no unificar** — `/api/delivery/*` (singular) es el canal de sesión-cookie para el panel admin (RBAC owner/admin/staff via `requireSession`), `/api/deliveries/*` (plural) es el canal de API-key para integraciones externas (`requireAuthMiddleware` + `requireScopeMiddleware(['orders:write'])`, mismo patrón que `/api/orders*` y `/api/b2b*`) — protegido explícitamente por la regla de "cero regresión" de la sección 5 (`:id/status`, `:id/photo`). Renombrar cualquiera de los dos rompería esa regla o el panel Despacho ya funcional, sin ganancia real: el problema original (frontend 404-eando) ya no existe. Verificado end-to-end con curl contra producción (cuentas `qa-test-s05-*@example.test` desechables): listar pendientes → 401 sin sesión, 200 con sesión; asignar repartidor → 401 sin sesión, 403 rol `viewer`, 200 rol `staff`; cambio reflejado en el GET siguiente (`status: assigned`, `driverId` seteado, `assignedAt` seteado). Nota: `apps/pos` tiene componentes de despacho (`dispatch-panel.tsx`, `assign-driver-modal.tsx`, etc.) que llaman además `GET /api/delivery/drivers`, `POST /api/delivery/dispatch-rappi`, `GET /api/delivery/payouts` — construidos en S07 (ver fila de abajo). |
| Turnos (historial cajero) | cerebro, pos | ✅ completo | S05 (1-sep-2026): los 3 endpoints (`POST /api/shifts/open`, `GET /api/shifts/active`, `GET /api/shifts/history`) ya existían (creados en sesiones previas del mismo día) y coinciden exactamente con el contrato que espera `apps/pos/src/components/pos/shift-gate.tsx` (`data.shift.{id,shiftNumber,openedAt,openingFloat,deviceId}`). No hizo falta escribir código — solo verificación. Verificado con curl contra producción: 401 sin sesión en los 3; `POST /open` con `device_id`+`opening_float_clp` devuelve 200 con el shape correcto; reabrir el mismo `device_id` devuelve 409 "Ya hay un turno abierto en este dispositivo"; `GET /active?device_id=` refleja el turno recién abierto; `GET /history` lista el turno con `cashierName`/`cashierEmail` correctos. Deuda ya documentada en sección 6.1 (no corregida aquí, fuera de alcance): estos 3 endpoints usan `getAuthUser()` (solo "¿hay sesión?"), no `requireSession(c, roles)` — cualquier rol autenticado puede abrir/ver turnos, no solo owner/admin/staff de la matriz. |
| B2B Crédito (solicitudes) | cerebro | ❌ falta | Cotizaciones (`b2b/quotes`) sí existen; solicitudes de crédito no |
| Seguridad (ARCOP Ley 21.719, void PIN, devoluciones) | cerebro, web | ✅ completo | S13 (2-sep-2026): `POST/GET /api/arcop`, `POST/GET /api/returns`. Ver "FASE 4 — S13" abajo. |
| Ajustes (PIN analytics, config tienda) | cerebro | ✅ completo | S06 (1-sep-2026): `GET/PUT /api/tienda-config/:key`, `GET /api/tienda-config/public` ya existían (construidos en una sesión temprana del mismo día, sin documentar formalmente) y coinciden exactamente con lo que `ajustes/page.tsx` necesita. No hizo falta escribir código — solo verificación. Verificado con Playwright contra producción: editar el PIN de analytics → `PUT /api/tienda-config/analytics_pin` → 200, mensaje "PIN actualizado correctamente" confirmado en UI. Nota: era la primera vez que se escribía esa clave (sin valor previo en `tienda_config`) — quedó en un valor de prueba, el dueño debe fijar el PIN real deseado. |
| POS: abrir/cerrar turno (till-sessions) | pos | ✅ completo | S06 (1-sep-2026): `POST /api/till-sessions/open`, `GET /api/till-sessions/active`, `GET /api/till-sessions/:id/z-report`, `POST /api/till-sessions/:id/close` ya existían (mismo caso que Turnos en S05 — construidos en una sesión temprana del mismo día) y coinciden exactamente con el contrato de `till-session-store.ts`, `till-gate.tsx` y `till-z-report-modal.tsx` (incluyendo el shape completo del Z-report: `ticketCount`, `byMethod`, `expectedCash`, etc.). No hizo falta escribir código — solo verificación. Verificado con Playwright contra producción: login POS → abrir turno (shift) → abrir caja (till) con fondo inicial → `POST /api/shifts/open` 200 + `POST /api/till-sessions/open` 200 → ambos gates desaparecen y queda la caja activa, confirmado visualmente. |
| POS: eventos en vivo (SSE) | pos | ✅ completo | S08 (1-sep-2026): `GET /api/events/pos` (roles owner/admin/staff). Fan-out de un solo `EventEmitter` en memoria (`packages/api/src/sse-broadcaster.ts`) — sin conexión Postgres ni `setInterval` de query por cliente SSE, para no repetir el "SSE pool exhaustion" ya resuelto una vez en el CRM de VÉRTICE. `POST /api/orders` emite `order.created` en el momento de crear el pedido (no polling). Verificado con curl `-N` contra producción (evento recibido con el shape exacto que espera `apps/pos/src/lib/order-events.ts`) y con Playwright real: pedido disparado en vivo apareció en el drawer "Pedidos entrantes" con badge de no-leído, "hace 2s". Prueba de carga: 10 conexiones SSE simultáneas (5 pos + 5 delivery), `/health` 200 en las 8 verificaciones durante la carga y después. Ver auditoría de cierre de Fase 2 abajo. |
| Repartidor: mis entregas, ubicación | repartidor | ✅ completo | S07 (1-sep-2026): `GET /api/delivery/assignments/mine` y `POST /api/delivery/location`, rol `delivery` únicamente, `driver_id` siempre de `authUser.id` de sesión (nunca de body/param). `mine` devuelve TODAS las asignaciones del repartidor (activas + entregadas/fallidas) — el frontend (`apps/repartidor/src/app/page.tsx`) hace su propio split cliente-side en tabs "Activos"/"Historial" desde el mismo array, confirmado leyendo el componente antes de escribir la query. `location` inserta en `delivery_location_pings` (tabla ya modelada en `packages/db/src/schema/delivery.ts`, nunca antes escrita por ningún endpoint). Verificado con curl contra producción (`https://api.seoulshop.cl`) con cuentas `qa-test-s07prod-*@example.test` desechables + 1 pedido + 1 asignación desechables: 401 sin sesión, 403 rol `staff` en ambos, 200 con datos reales para rol `delivery`. **Nota — deuda encontrada, no corregida (fuera de alcance S07):** `apps/repartidor/src/app/page.tsx` también llama `PUT /api/delivery/assignments/:id/status` y `POST /api/delivery/assignments/:id/pod`, que NO coinciden con ninguna ruta registrada (las rutas reales son `POST /api/deliveries/:id/status` y `POST /api/deliveries/:id/photo`, plural, sin `/assignments/`) — el repartidor puede ver y aceptar sus entregas pero no puede marcarlas como recogidas/entregadas ni subir el POD hoy. Bug pre-existente a esta sesión, no introducido por S07 — candidato claro para S08 o una sesión de hotfix. |
| Despacho: repartidores/Rappi/liquidaciones | cerebro, pos | ✅ completo | S07 (1-sep-2026): `GET /api/delivery/drivers` (rol owner/admin/staff, `activeJobs` = asignaciones en `assigned`/`accepted`/`in_transit`, consumido por `assign-driver-modal.tsx`, `dispatch-panel.tsx`, `dispatch-bifurcation-panel.tsx`). `POST /api/delivery/dispatch-rappi` — **sin integración real con la API de Rappi** (sin credenciales configuradas en el proyecto hoy), registra en `delivery_assignments` (`dispatch_type='rappi'`, `third_party_name`, `third_party_tracking`, ya modelados en `migrate-0009`) que staff despachó manualmente por Rappi; crea la asignación si el pedido no tenía una, actualiza (no duplica) si ya existía — verificado con ambos casos. `GET /api/delivery/drivers/:id/z-report` + `GET`+`POST /api/delivery/payouts` (rol owner/admin/staff, consumidos por `driver-z-report-modal.tsx` en Ajustes del POS) — el "Liquidar turno" marca `paid_at=NOW()` de inmediato (no hay paso de aprobación separado en la UI). **Limitación conocida, documentada en el código, no falseada:** `distancia_km`/`monto_repartidor_clp` en `delivery_assignments` están modelados desde `migrate-0009` pero ningún endpoint (incluyendo esta sesión) los escribe todavía — no hay lógica de cálculo de distancia (requeriría derivarla de los pings de GPS que esta misma sesión empezó a guardar, o una API de mapas). `totalKm`/`grossClp` leen 0 hoy para todos los repartidores hasta que una sesión futura agregue ese cálculo; `netPayable` es 0 menos el efectivo cobrado. Verificado con curl contra producción: 401 sin sesión, 403 rol `delivery` en los 4, 200 con datos reales para roles correctos; `z-report` avanza correctamente el `periodFrom` del próximo cálculo tras cada `POST /payouts` (verificado registrando un payout y confirmando que el siguiente z-report arranca desde `period_to` del anterior). Cero campo de costo/margen expuesto. Grep confirmó `apps/web` no consume ningún endpoint de `/api/delivery/*`. Cuentas y datos de prueba (`qa-test-s07*@example.test`, 3 pedidos, 3 asignaciones, 1 ping, 2 payouts) borrados al cerrar, verificado con SELECT posterior sin filas restantes. `/health` en 200 antes y después del push. Playwright no se ejecutó esta sesión (deliberado, no bloqueante — ver `SEUL_SESSION_07.md` para el razonamiento): ningún componente frontend nuevo fue tocado, los 4 consumidores ya existían y ya tenían `credentials: 'include'`, y la superficie completa es JSON puro verificado campo por campo contra los `interface`/`type` reales de cada componente TSX. |
| Repartidor: eventos en vivo (SSE) | repartidor | ✅ completo | S08 (1-sep-2026): `GET /api/events/delivery` (rol `delivery` únicamente). Targeting siempre por `authUser.id` de sesión (nunca broadcast ni id de cliente) — mismo invariante que `assignments/mine`/`location` de S07, porque el frontend acepta la alerta con el `assignmentId` exacto que llega en el evento. `PUT /api/delivery/assignments/:id/assign` emite `order.ready_for_dispatch` dirigido solo al repartidor asignado en el momento del assign (no polling). Verificado con curl `-N` y con Playwright real: al asignar desde staff, el `DispatchAlertOverlay` apareció en vivo en el navegador del repartidor con los datos correctos (#37915, $5.000, cobrar en puerta), "ACEPTAR VIAJE" avanzó el estado, y el ciclo completo (recogido → en camino → entregado) funcionó con ping GPS real insertado en `delivery_location_pings`. Ver auditoría de cierre de Fase 2 abajo. |
| Tienda web: login/registro cliente | web | ✅ completo | S09 (1-sep-2026): `POST /api/customer/{login,register}`, `GET /api/customer/me`, `POST /api/customer/logout`, `POST /api/customer/password-{change,forgot,reset}`. Ver detalle completo en "FASE 3 — S09" abajo. El hallazgo 🔴 de la carpeta `app/` duplicada (bloqueaba TODA ruta de `src/app/`) ya se había resuelto antes de S10 (commits `23aa08f`, `b550c2b`, mismo día) — S10 encontró un tercer bloqueador del mismo síntoma (ver fila de abajo) y lo cerró. Verificado en navegador real (Playwright, Chrome) contra producción: `/cuenta/login` carga 200. |
| Tienda web: catálogo público | web | ✅ completo | S03: `GET /api/products` (listado). S10 (1-sep-2026) agregó `GET /api/products/:slug` (detalle público, contraparte de `/api/products/id/:id` que es staff-only) — consumido por `apps/web/.../producto/[slug]/page.tsx`. **🔴 Hallazgo crítico adicional, arreglado en S10:** `apps/web/src/middleware.ts` (preexistente, no relacionado con los duplicados de `app/`/`page.tsx` ya resueltos) redirigía TODA request a `/` → `/productos` con 307 — la homepage real en `(shop)/page.tsx` nunca era alcanzable en producción pese a que su código y el build de Vercel eran correctos. Confirmado con `curl -I https://seoulshop.cl/` (307 antes / 200 después) y Playwright real (título de página y contenido visual = homepage editorial, no el listado de Productos). Middleware eliminado (no tenía otro propósito) — commit `e85bf37`. Verificado con curl contra producción con datos reales (77+ productos) y Playwright real: `/`, `/productos` (96 tarjetas), `/producto/beb-012` (detalle con precio y galería) cargan 200 con contenido correcto. |
| Tienda web: mis pedidos | web | ✅ completo | S10 (1-sep-2026): `GET /api/customer/orders`, filtra siempre por el `customerId` de `requireCustomerSession` (nunca por parámetro). Consumido por `apps/web/.../cuenta/pedidos/page.tsx` (ya existía desde S09, solo esperaba el backend). Verificado con curl end-to-end contra producción (cuenta `qa-test-s10-*@example.test` desechable): pedidos vacíos tras registro → aparece el pedido recién creado tras `POST /api/public/orders` → borrado todo al cerrar. |
| Tienda web: checkout público (pedido de cliente/invitado) | web | ✅ completo | S10 (1-sep-2026): `POST /api/customers/guest` (upsert "reclama fantasma", mismo patrón que `/api/customer/register`) + `POST /api/public/orders` (crea el pedido, vincula `customer_id` SIEMPRE desde la sesión de cliente cuando existe — nunca del body — y recalcula `unitPrice` server-side desde `products.price_retail`, nunca confía en el precio que manda el navegador). El frontend (`createWebOrder` en `apps/web/src/lib/api.ts`) apuntaba a `POST /api/orders/public`, que **nunca existió** — el checkout estaba roto end-to-end desde que se escribió el código, no solo bloqueado por el hallazgo de rutas. `/api/public/orders` vive deliberadamente fuera del prefijo `/api/orders*` (exige API key o sesión staff, ver `requireAuthMiddleware`) para que un visitante anónimo pueda crear su propio pedido. No descuenta inventario (deuda preexistente, ningún endpoint del sistema lo hace hoy) ni genera PDF/boleta (`pdfToken` se devuelve `null` — es la Fase de SII/DTE, pospuesta post-entrega). Verificado con curl (spoofing de `customerId` y de `unitPrice` confirmado bloqueado — el pedido quedó con el `customer_id` real de la sesión y el precio real de catálogo, no los valores manipulados del request) y con Playwright real de punta a punta: agregar al carrito en `/producto/beb-012` → `/checkout` → llenar datos de invitado → confirmar → pantalla "¡Pedido #N enviado!" → confirmado en Postgres (`orders`/`order_items` con los valores correctos) → datos de prueba borrados al cerrar. |
| Portal B2B (empresa): catálogo, wallet, pedidos | web | ✅ completo | S11 (2-sep-2026): 7 endpoints (`registro`, `catalogo`, `empresa/me`, `credit-request`+`credit-requests/:id`+`:id/review`, `solicitudes`, `pedidos/:id`, `wallet/:id`). Reutiliza la sesión de cliente B2C (`requireCustomerSession` + `b2b_companies.customer_id`), no un login propio. Verificado con curl + Playwright real contra producción — ver "FASE 3 — S11" y "Auditoría de cierre — FASE 3". |
| Boleta electrónica (SII/DTE) | — | ❌ falta | Schema listo (`dte-events.ts`), riesgo legal #1 del plan de negocio — pospuesto post-entrega |
| Analítica (Ventas de caja POS + PIN) | pos | ✅ completo | S14 (2-sep-2026): `POST /api/analytics/pin-check`, `GET /api/analytics/sales`. Ver "FASE 4 — S14" abajo. |
| FAQ pública | web | ✅ completo | S14 (2-sep-2026): `GET /api/faq`. Ver "FASE 4 — S14" abajo. |
| Promociones, fidelización | — | ❌ falta (decisión pendiente) | Schemas y tablas ya en producción, cero UI/rutas/consumidor. Evaluado en S14 — recomendación: postergar a v1.1, ver "FASE 4 — S14" abajo. |

**Leyenda:** ✅ funciona y verificado · 🟡 parcial/roto · ❌ sin construir

### 6.1 Matriz de roles → secciones visibles (definida en S01, implementada en S02)

Los 5 roles ya existen en `VALID_ROLES` (`packages/api/src/server.ts`, handler de `/api/auth/register`): `owner`, `admin`, `staff`, `delivery`, `viewer`.

| Rol | Acceso |
|---|---|
| `owner` | Todo — todas las secciones de `cerebro`, sin restricción. |
| `admin` | Todo excepto **Usuarios** y **Seguridad**. |
| `staff` | Solo **Comandas**, **Despacho**, **Turnos**, **Clientes**. |
| `delivery` | Solo el módulo de repartidor (app `apps/repartidor`) — sin acceso a `cerebro`. |
| `viewer` | Solo lectura de **Dashboard** y **Reportes** — ninguna acción de escritura. |

**Estado S02 (implementado y verificado en producción, 31-ago/1-sep-2026):**

- **Frontend** (`apps/cerebro/src/components/layout/sidebar.tsx`): cada item del `nav[]` ahora lleva un array `roles` y la lista se filtra con `nav.filter(item => item.roles.includes(user.role))` antes de renderizar. Verificado con Playwright contra `cmr.seoulshop.cl` real: una cuenta `staff` desechable solo ve Comandas/Clientes/Despacho/Turnos (+ POS Caja/Tienda Web externos); una cuenta `owner` desechable ve las 11 secciones incluyendo Usuarios y Seguridad. Screenshots verificados visualmente.
  - Nota: `delivery` nunca llega a este componente porque `apps/cerebro/src/app/(admin)/layout.tsx` ya gatea todo el route group a `['owner','admin','staff']` — pero eso también bloquea a `viewer`, que según la matriz debería poder entrar solo a Dashboard/Reportes. Es un gap preexistente (no introducido en S02) que queda documentado como deuda, no bloqueante — no hay hoy ninguna cuenta `viewer` real en producción.
  - También sigue pendiente: la página `/usuarios` (y las demás rutas ocultas del Sidebar) no tiene guard de ruta a nivel de página — un `staff` que navegue directo a la URL no ve el link pero tampoco es redirigido. El backend sí bloquea las acciones reales (ver abajo), así que no hay fuga de datos, solo una pantalla que renderizaría vacía/con 403 en sus fetch. Deuda documentada, no bloqueante para S02.

- **Backend** — migrados a `requireSession(c, roles)` con validación de rol real en S02:
  - `GET /api/auth/users` → `['owner','admin','staff']` (no owner-only: también alimenta el selector de repartidor de Despacho, al que staff/admin tienen acceso legítimo — restringirlo a owner habría regresionado esa pantalla).
  - `PUT /api/auth/users/:id` → `['owner']`
  - `DELETE /api/auth/users/:id` → `['owner']`
  - `POST /api/auth/register` → `['owner']`
  - `PUT /api/delivery/assignments/:id/assign` → `['owner','admin','staff']`
  - Los 5 endpoints anteriores verificados con curl contra producción usando cuentas `qa-test-*@example.test` desechables (creadas y borradas en la misma sesión): `staff` recibe 403 en PUT/DELETE users y en register, 200/404 (no 403) en GET users y en delivery/assign; `owner` recibe 200/404 en todo.
  - **Deuda pendiente (no bloqueante, sesiones futuras):** el resto de endpoints de hoy siguen solo con `getAuthUser()` → "¿hay sesión?" sin validar rol: `POST /api/shifts/open`, `POST /api/shifts/:id/close`, `POST/GET /api/till-sessions/*`, `PUT /api/tienda-config/:key`, `POST /api/deliveries/assign` (legacy), `POST /api/deliveries/:id/status`, `POST /api/deliveries/:id/photo`, `POST /api/orders/:id/status`. Ninguno de estos está marcado como restringido-por-rol en la matriz de arriba salvo por pertenecer a secciones ya ocultas en el Sidebar (Turnos, Ajustes, Despacho) — el riesgo real es bajo (todos exigen sesión válida) pero no hay defensa en profundidad por rol todavía.

## 7. Gap completo de endpoints (auditado 31-ago-2026)

Metodología: `grep` de todos los `fetch()` a `${API_URL}/api/...` en las 4 apps, cruzado contra las rutas registradas en `server.ts`.

**Ya existen (18):** `/api/auth/{login,logout,me,register,change-password,users,users/:id}`, `/api/orders` (solo POST — crear), `/api/orders/:id/status`, `/api/b2b/quotes` (+ `:id/accept`, `:id/reject`), `/api/deliveries/{assign,:id/status,:id/photo}`, `/api/admin/api-keys` (+ `:id/revoke`), `/api/admin/seed/users`, `/api/email-queue/:id`.

**Faltan (≈27), agrupadas por dominio:**

- **Dashboard/Analítica:** `GET /api/dashboard/stats`, `GET /api/dashboard/alerts` (✅ S04) · ~~`GET /api/analytics/sales`, `POST /api/analytics/pin-check`~~ — ✅ construidos en S14
- **Productos:** `GET /api/products`, `GET /api/products/meta/categories`, `GET /api/products/barcode/:code`, `GET /api/products/id/:id`
- **Inventario:** `GET /api/inventory`
- **Clientes (CRM interno):** `GET /api/customers`, `GET /api/customers/search`, `GET /api/customers/:id`, `GET /api/customers/:id/timeline`
- **Comandas:** `GET /api/orders/comandas`, `GET /api/orders` (listar, no solo crear)
- **Despacho / Delivery (admin):** `GET /api/delivery/assignments`, `POST /api/delivery/assignments/:id/assign` — **renombrar o alinear con `/api/deliveries/*` existente**, `GET /api/delivery/drivers`, `POST /api/delivery/dispatch-rappi`, `GET /api/delivery/payouts`
- **Turnos / Till-sessions:** `GET /api/shifts/history`, `GET /api/shifts/active`, `POST /api/shifts/open`, `GET /api/till-sessions/active`, `POST /api/till-sessions/open`
- ~~**B2B Portal:** `GET /api/b2b/catalogo`, `POST /api/b2b/registro`, `GET /api/b2b/empresa/me`, `POST /api/b2b/credit-request`, `GET /api/b2b/credit-requests/:id`, `GET /api/b2b/pedidos/:id`, `GET /api/b2b/wallet/:id`, `GET /api/b2b/solicitudes`~~ — ✅ construidos en S11 (más `PATCH /api/b2b/credit-requests/:id/review`, descubierto como necesario por grep contra `apps/cerebro/.../b2b/solicitudes/page.tsx`)
- **Cliente final (tienda web):** `POST /api/customer/{login,register}`, `GET /api/customer/me`, `GET /api/customer/orders`, `POST /api/customer/password-{change,forgot,reset}`
- **Ajustes / Config:** `GET/PUT /api/tienda-config`, `GET/PUT /api/tienda-config/analytics_pin`, `GET /api/tienda-config/public`, `POST /api/tienda-config/void_pin`
- ~~**Seguridad / Legal:** `GET /api/arcop`, `GET /api/returns`~~ — ✅ construidos en S13 (más `POST /api/arcop` y `POST /api/returns`, públicos, descubiertos por grep contra `apps/web/.../privacidad/page.tsx` y `apps/web/.../devoluciones/page.tsx`, que ya los llamaban)
- **Repartidor:** `GET /api/delivery/assignments/mine`, `POST /api/delivery/location`
- **Tiempo real (SSE):** `GET /api/events/pos`, `GET /api/events/delivery`
- ~~**Otros:** `GET /api/faq`~~ — ✅ construido en S14

---

## FASE 0 — Seguridad y Estabilización (S01-02, 16h, 🔴 BLOQUEADORA)

No se avanza a Fase 1 sin cerrar esto: es la base sobre la que se construyen ~25 endpoints nuevos.

**S01 (8h):**
- Revisar y commitear (o descartar) el diff pendiente de `/api/auth/users` (bloqueador P0 #7).
- `JWT_SECRET`: throw en producción si falta, igual que `DATABASE_URL` (bloqueador P0 #1).
- Crear `requireSession(c, roles?)` único en `middleware/auth.middleware.ts` que reemplace el patrón copy-pasted de `getAuthUser` (bloqueador P0 #2). Migrar los endpoints existentes a usarlo.
- Definir la matriz de roles → secciones visibles (owner: todo · admin: todo menos Usuarios/Seguridad · staff: Comandas/Despacho/Turnos/Clientes · delivery: solo repartidor · viewer: solo lectura Dashboard/Reportes). Documentarla en este archivo (sección 6 se actualiza con esto).

**S02 (8h) — ✅ COMPLETA (1-sep-2026):**
- ✅ RBAC en `Sidebar` de cerebro (ocultar secciones según `role`) — ver detalle y deuda pendiente en sección 6.1.
- ✅ RBAC server-side con `requireSession(c, roles)` en los 5 endpoints más sensibles de hoy (Usuarios GET/PUT/DELETE, register, delivery/assign) — ver sección 6.1 para la lista completa migrada vs. deuda pendiente.
- ✅ Rate limiter genérico en Postgres (tabla `rate_limit_events`, migración 0016, función `checkAndRecordRateLimit` en `server.ts`) — reemplaza el TODO de KV en `auth.middleware.ts:160`. Aplicado a `POST /api/orders`, `POST /api/b2b/quotes`, `POST /api/auth/register` (20 req / 5 min por usuario o IP). Verificado en producción: 20 requests pasan, el 21° responde `429` con `{"error":"Demasiadas solicitudes. Intenta de nuevo en 5 minutos."}`.
- ✅ **Gate de fase:** smoke test con Playwright contra producción real (`cmr.seoulshop.cl`, `pos.seoulshop.cl`, `drive.seoulshop.cl`) usando cuentas `qa-test-*@example.test` desechables (creadas directo en Neon, nunca vía email real, borradas al cerrar la sesión junto con toda la data de prueba que generaron — órdenes, cotización, email_queue/email_log, rate_limit_events). Login OK en las 3 apps. RBAC del Sidebar confirmado: `staff` ve solo Comandas/Clientes/Despacho/Turnos, `owner` ve las 11 secciones. `/health` en 200 después de cada push.
- Commits: `9c0adfe` (Sidebar RBAC), `6084177` (RBAC server-side), `10da8d9` (rate limiter genérico) — pusheados a `main`, auto-deploy Railway confirmado.

## FASE 1 — Núcleo de Operación Interna: Panel Admin + POS (S03-06, 64h)

El corazón del negocio diario. Sin esto, el dueño no puede operar la tienda desde el sistema.

**S03 (16h) — Productos + Inventario — ✅ COMPLETA (1-sep-2026):**
`GET /api/products`, `/api/products/meta/categories`, `/api/products/barcode/:code`, `/api/products/id/:id`, `GET /api/inventory`. Usó `packages/db/src/schema/products.ts` e `inventory.ts` tal cual están modelados. Todos con `requireSession(c)` sin restricción de rol (POS/cerebro/web comparten catálogo). `stockTotal` se calcula en vivo con `SUM(inventory.quantity)` por producto — se descubrió que `inventory_summary` (la tabla derivada pensada para esto) nunca tuvo su migración/trigger aplicados en producción (0 filas), así que no se usó. Semáforo de vencimiento (`expiryStatus` en `/api/inventory`) usa los mismos umbrales que `packages/ui/src/badge-expiry.tsx`. Verificado con curl contra producción con datos reales (81 productos, 65 lotes, 20 categorías) usando cuentas `qa-test-s03*@example.test` desechables (creadas y borradas en la misma sesión). Playwright no se pudo ejecutar (el Chromium/Firefox de la versión instalada no soporta macOS 13 de esta máquina) — deuda de verificación visual, no bloqueante dado el curl exhaustivo. Commits: `4b0c0c3`, `235402b`, `f0948c6`.

**S04 (16h) — Comandas + Dashboard — ✅ COMPLETA (1-sep-2026):**
`GET /api/orders` (listar con filtros status/channel/limit), `GET /api/orders/comandas` (Kanban), `GET /api/dashboard/stats`, `GET /api/dashboard/alerts`. Todos con `requireSession(c, roles)` — Comandas es owner/admin/staff, Dashboard es owner/admin/viewer, siguiendo exactamente la matriz de sección 6.1 y el `nav[]` de `sidebar.tsx`. Se confirmó por grep en las 4 apps que ni Comandas ni Dashboard son consumidos por `apps/web` (tienda pública) — a diferencia de products/categories en S03, no hizo falta el patrón de sesión opcional. Verificado con curl contra producción (Neon real) con cuentas `qa-test-s04*@example.test` desechables (creadas y borradas dos veces — una vez local contra la DB de prod antes del deploy, otra vez después del push para probar el deploy real): 401 sin sesión, 403 para el rol no autorizado en cada endpoint, 200 con datos reales para los roles correctos. Cero campo de costo/margen (`costPrice`/`priceB2B`/`pricePOS`) en ninguna respuesta. `/health` en 200 después del push. Playwright: se intentó instalar Chromium (`npx playwright install chromium`) para verificar en navegador real que el banner "API no disponible" desaparece — no completó en esta máquina (mismo problema reportado en S03, macOS 13), documentado como deuda de verificación visual, no bloqueante dado el curl exhaustivo contra producción. Deuda encontrada y documentada (no corregida, fuera de alcance): `comandas/page.tsx` mueve tarjetas con `PATCH /api/orders/:id/status`, pero ese endpoint existente solo acepta `POST` y el CORS no permite `PATCH` — el listado de Comandas carga, el drag/move de una tarjeta no. Commits: `5eb1225`, `fdad620`, `43d7ff8`.

**S05 (16h) — Despacho + Turnos — ✅ COMPLETA (1-sep-2026):**
Ambas piezas resultaron ya construidas por sesiones previas del mismo día (S02 para Despacho vía commit `6084177`; Turnos vía sesiones anteriores no documentadas formalmente) — el trabajo de S05 fue verificación exhaustiva end-to-end y una decisión de arquitectura documentada, no escribir endpoints nuevos.

- **Despacho:** el mismatch de paths de la sección 4/bloqueador P0 #5 ya no existe en la práctica — `GET /api/delivery/assignments` + `PUT /api/delivery/assignments/:id/assign` (singular) son lo único que `apps/cerebro`, `apps/pos` y `apps/repartidor` llaman (confirmado por grep, cero uso de `/api/deliveries/*` plural en las 4 apps). **Decisión: mantener ambos prefijos** — no es duplicación sin sentido, son dos audiencias distintas (sesión-cookie admin vs. API-key externo) y la sección 5 protege explícitamente `/api/deliveries/:id/status`/`:id/photo` de regresión. Ver detalle completo en la fila "Despacho" de la sección 6.
- **Turnos:** `POST /api/shifts/open` ya existía (no fue necesario escribirlo) y coincide exactamente con el contrato de `shift-gate.tsx`. Ver detalle completo en la fila "Turnos" de la sección 6.
- **Verificación end-to-end (curl contra producción, `https://api.seoulshop.cl`):** cuentas desechables `qa-test-s05-{owner,staff,viewer,driver}@example.test` (creadas por SQL directo vía script tsx, borradas al cerrar junto con el pedido/turno/asignación de prueba que generaron). 401 sin sesión en todos los endpoints tocados; 403 para rol no autorizado (`viewer` en assign); 200 con datos reales para el rol correcto; cambio de estado confirmado reflejado en el siguiente GET. Cero campo de costo/margen en las respuestas de Despacho. `/health` en 200 antes y después de la sesión (no hubo push de código, solo documentación).
- **Playwright:** se intentó `npx playwright install chromium` de nuevo — falla con `ERROR: Playwright does not support chromium on mac13` (3ª sesión consecutiva con el mismo bloqueo de esta máquina, ver S03/S04). No bloqueante dado el curl exhaustivo.
- Sin commits de código (no hubo cambios a `server.ts` — todo lo pedido ya existía). Commit único de este cierre: actualización de este plan + `SEUL_SESSION_05.md`.

**S06 (16h) — POS: Till-sessions + Ajustes — ✅ COMPLETA (1-sep-2026):**
Ambas piezas resultaron ya construidas por una sesión temprana del mismo día (mismo patrón que Despacho/Turnos en S05) — el trabajo de S06 fue verificación de contrato backend↔frontend (sección 6) más la auditoría de cierre de Fase 1 obligatoria (ver sección nueva más abajo), que encontró y corrigió un bug real en Comandas que ninguna sesión anterior había detectado porque solo se había verificado con curl. **Gate de fase (cumplido):** las 6 pantallas que fallaron en las capturas originales (Comandas, Despacho, Turnos, Usuarios, Ajustes, Abrir turno POS) cargan sin error en navegador real — verificado con Playwright contra producción, no solo curl. Ver "Auditoría de cierre — FASE 1" abajo para el detalle completo pantalla por pantalla, incluyendo botones/acciones probados. Commit de código: `f32a532` (fix Comandas). Commit de documentación: este mismo cierre.

---

## Auditoría de cierre — FASE 1 (1-sep-2026)

Requisito explícito del dueño del negocio: "cada vez que finalices una fase debe ser auditada y probada para verificar que no falte ninguna pestaña, ningún proceso, ninguna acción o botón importante". Esto es distinto del gate de sesión normal — es un barrido de TODO lo que Fase 1 tocó (S03-S06), en navegador real (Playwright con `channel: 'chrome'`, Chrome real del sistema — el Chromium embebido de Playwright sigue sin soportar este macOS 13, mismo bloqueo reportado en S03-S05, pero `channel: 'chrome'` funcionó sin problema en toda la auditoría), usando una cuenta owner desechable (`qa-test-fase1-close@example.test`, creada por SQL directo, password-hash y `must_change_password=false` seteados a mano — nunca por email real) y dos pedidos + una asignación de despacho desechables creados por SQL para no tocar datos de clientes reales. Toda la cuenta y los datos de prueba fueron borrados al cerrar la auditoría (verificado con SELECT posterior, sin filas restantes).

**Resultado: 8/8 pantallas cargan sin error Y su botón/acción principal funciona en navegador real.** Se encontró y arregló 1 bug real (Comandas) que las sesiones anteriores no habían detectado porque solo habían verificado con `curl`.

| Pantalla | Carga sin error | Botón/acción principal funciona | Notas / hallazgos |
|---|---|---|---|
| Comandas | ✅ (tras fix) | ✅ mover tarjeta: `Preparar` → `PATCH /api/orders/:id/status` → 200, `↩` (volver a Nueva) → 200 | **Bug encontrado y arreglado**: `fetch()` sin `credentials: 'include'` → 401 en navegador real (cross-origin `cmr.seoulshop.cl` → `api.seoulshop.cl`) + crash JS "Cannot read properties of undefined". `curl -b cookies.txt` nunca lo detectó porque adjunta la cookie a mano. Fix de 2 líneas, commit `f32a532`. |
| Despacho | ✅ | ✅ asignar repartidor: `select` → `PUT /api/delivery/assignments/:id/assign` → 200, reflejado en la lista tras recargar | Sin cambios de código — ya funcionaba (S05). |
| Turnos | ✅ | ✅ historial carga (1 turno real visible en la lista) | Sin cambios de código — ya funcionaba (S05). Acción "abrir turno" se prueba desde POS, no desde esta pantalla (ver fila POS abajo). |
| Usuarios | ✅ | ✅ crear (`POST /api/auth/register` → 200, usuario visible en la tabla), editar rol (`PUT /api/auth/users/:id` → 200, badge cambia de Staff a Admin), desactivar (`PUT` → 200, badge cambia a Inactivo) — los 3 confirmados visualmente | El botón "Eliminar" (ícono papelera) es en realidad un soft-delete — el backend (`server.ts:713-731`) hace `UPDATE users SET is_active = false`, no un `DELETE` real. El diálogo de confirmación ya lo dice ("Esta acción lo desactivará del sistema"), pero el ícono de papelera + label "Eliminar usuario" puede hacer pensar al dueño que borra la cuenta de verdad. No es un bug — es el mismo efecto que "Desactivar" con una etiqueta engañosa. Nota de UX para Fase 5 (S15, limpieza de copy), no se tocó. |
| Ajustes | ✅ | ✅ editar PIN de analytics: `PUT /api/tienda-config/analytics_pin` → 200, mensaje "PIN actualizado correctamente" confirmado en UI | Era la primera vez que se escribía esa clave en `tienda_config` (sin valor previo) — quedó con un valor de prueba tras la auditoría. El dueño debe fijar el PIN real que quiere usar en el POS. |
| POS: abrir turno | ✅ | ✅ abrir turno (shift): `POST /api/shifts/open` → 200 → abrir caja (till): `POST /api/till-sessions/open` → 200 → ambos gates (Apertura de Turno / Apertura de Caja) desaparecen y queda la pantalla principal del POS, confirmado visualmente | Sin cambios de código. Turno/caja de prueba borrados de la base al cerrar. |
| Dashboard | ✅ | ✅ KPIs y alertas cargan sin el banner "API no disponible" ni crash | Sin cambios de código — ya funcionaba (S04). |
| Productos (S03, re-verificada) | ✅ | ✅ 81 productos reales listados | Server Component (`getProducts()` en `apps/cerebro/src/lib/api.ts`) — el fetch corre en el servidor de Next.js, no en el navegador, así que el bug de `credentials` de Comandas no aplica aquí (la cookie se reenvía a mano vía `Cookie:` header server-to-server). |
| Inventario (S03, re-verificada) | ✅ | ✅ 65 lotes reales listados con semáforo de vencimiento | Mismo patrón Server Component que Productos — no afectado por el bug de Comandas. |

**Pestañas visibles en el Sidebar que Fase 1 NO cubre (reportado, no arreglado — confirmado que siguen sin backend):**
- **Clientes** (`/clientes`, visible para owner/admin/staff) — sin ningún `fetch()` en la página, es un placeholder. Backend (`GET /api/customers*`) no existe. Ya documentado como gap en sección 7, Fase 3.
- **B2B Crédito** (`/b2b/solicitudes`, visible para owner/admin) — backend `GET /api/b2b/solicitudes` no existe. Ya documentado como gap en sección 7, Fase 3.
- **Seguridad** (`/seguridad`, visible solo para owner) — parcialmente rota, hallazgo nuevo de esta auditoría:
  - `getARCOP()` y `getReturns()` llaman `GET /api/arcop` y `GET /api/returns` — no existen (Fase 4/S13, ya documentado en sección 7).
  - `getVoidPin()` llama `GET /api/tienda-config` (**sin key**) — no coincide con la ruta registrada `GET /api/tienda-config/:key`, así que siempre falla y la página muestra el PIN por defecto hardcodeado `'abcd'` en vez del PIN real guardado. Es un bug nuevo (nadie lo había encontrado porque Seguridad no estaba en el alcance de S01-S06), pero el formulario para *cambiar* el PIN maestro (`pin-maestro-section.tsx`, `PUT /api/tienda-config/void_pin`) sí funciona bien — solo la lectura del valor actual está rota. No se corrigió: Seguridad no es una de las 6 pantallas del gate de S06 y su backend restante es explícitamente Fase 4. Queda para cuando se aborde Seguridad de lleno.

**Oportunidades de optimización encontradas (documentadas, no implementadas por decisión explícita — requieren decisión del dueño o son more-than-trivial):**
1. `apps/cerebro/src/lib/api.ts` (`serverFetch`) tiene un timeout de 6s que, si se cumple, hace fallback silencioso a lista vacía (`{ products: [], total: 0 }` etc.) — indistinguible en la UI de "no hay productos". Dashboard sí tiene un banner "API no disponible" para este caso; Productos/Inventario no. Vale la pena el mismo patrón ahí.
2. `GET /api/auth/users` no pagina — hoy son pocos usuarios, pero Despacho y Usuarios lo llaman completo cada vez; si el staff crece a decenas esto empieza a pesar. No urgente para v1.0.
3. El label "Eliminar usuario" (ver fila Usuarios arriba) debería decir "Desactivar" para no prometer un borrado que no ocurre — 1 línea de copy, dejado para el pase de limpieza de UX de Fase 5 (S15) en vez de tocarlo suelto ahora.

---

## Adición post-Fase 1 — Comandas dentro de POS (1-sep-2026)

Fuera de la numeración S01-S06 — tarea puntual pedida por el dueño, no una sesión de fase. Decisión de producto: la cajera (rol `staff`) no tiene acceso a `cmr.seoulshop.cl` en el flujo real de trabajo, solo usa `pos.seoulshop.cl`. Necesitaba ver y gestionar Comandas sin salir de POS.

Se agregó una vista Comandas dentro de `apps/pos` (`apps/pos/src/components/pos/comandas-view.tsx`, más un botón `title="Comandas"` en el TopBar y el estado que lo abre en `apps/pos/src/app/page.tsx`) que reusa **sin ningún cambio** el backend ya construido en S04: `GET /api/orders/comandas` y `PATCH /api/orders/:id/status`. Adaptada a tablet táctil: Kanban de 3 columnas a pantalla completa con un solo botón "Siguiente estado" por tarjeta (nueva → preparando → lista) en vez del drag-and-drop del Kanban de cerebro, botones con `min-height: var(--pos-hit-area-min)`, y `credentials: 'include'` en todos los fetch desde el día uno (para no repetir el bug de cookie cross-origin que se encontró y arregló hoy mismo en `cerebro/comandas`, commit `f32a532`). No se tocó `apps/cerebro/.../comandas/page.tsx` ni el backend de orders/comandas.

Commit: `ae42995`. `type-check` de `@seul/pos` limpio antes de commitear.

**Verificación:** curl confirmó el contrato sin cambios (401 sin sesión, 200 con sesión `staff` real, PATCH mueve el estado). Playwright (`channel: 'chrome'`) contra `pos.seoulshop.cl` en producción, con una cuenta `qa-test-pos-comandas@example.test` desechable (creada por SQL directo) y un pedido de prueba desechable (`guest_name = 'QA Test Comandas'`, insertado por SQL directo, no vía checkout real): login OK, apertura de turno y caja OK, botón Comandas visible y abre el Kanban a pantalla completa, columna "Nueva" con el pedido de prueba visible, click en "Comenzar preparación" → `PATCH /api/orders/:id/status` → 200 → tarjeta se mueve a "Preparando", cierre de la vista OK. Cuenta, turno, caja y pedido de prueba borrados de la base al cerrar (verificado con SELECT posterior, sin filas restantes).

**Incidente durante la verificación (corregido en el momento, no llegó a quedar en la base):** el primer intento de Playwright usó `getByRole('button', { name: 'Comandas' })`, que en Playwright hace *substring match* por defecto — coincidió con el botón de avatar de la cajera ("QA Test Comandas POS", que contiene "Comandas" como substring) en vez del botón nuevo del TopBar, y el click resultante cayó sobre la **primera** tarjeta real de la columna "Nueva" (pedido real `#53156`, no el de prueba), moviéndolo a "Preparando" en producción. Se detectó de inmediato al revisar el log de red del script (el `PATCH` fue contra un id de orden que no era el de prueba) y se revirtió al toque con un `PATCH .../status {"status":"nueva"}` contra ese mismo pedido — confirmado con un `GET /api/orders/comandas` posterior que volvió a aparecer en "Nueva". El script se corrigió para usar un selector inequívoco (`button[title="Comandas"]`) antes de repetir la verificación. Ningún dato de cliente real quedó alterado, pero es la razón por la que esta nota documenta el incidente explícitamente en vez de solo el resultado final.

---

## FASE 2 — Repartidor + Tiempo Real (S07-08, 28h)

**S07 (16h) — ✅ COMPLETA (1-sep-2026):** `GET /api/delivery/assignments/mine`, `POST /api/delivery/location`, `GET /api/delivery/drivers`, `POST /api/delivery/dispatch-rappi`, `GET /api/delivery/payouts` (GET+POST) — más `GET /api/delivery/drivers/:id/z-report`, no listado explícitamente en este plan pero descubierto como consumidor real necesario (`driver-z-report-modal.tsx`) para que el flujo de "Liquidar turno" del punto 5 funcione de punta a punta. Ver detalle completo (incluyendo la limitación documentada de `distancia_km`/`monto_repartidor_clp` sin poblar, y el bug pre-existente de `status`/`pod` del repartidor, no corregido por estar fuera de alcance) en las filas "Repartidor" y "Despacho: repartidores/Rappi/liquidaciones" de la sección 6, y en `SEUL_SESSION_07.md`. Commit `75ff879`.

**S08 (12h) — ✅ COMPLETA (1-sep-2026):** `GET /api/events/pos` y `GET /api/events/delivery` (SSE) — resuelve el `ERR_CONNECTION_REFUSED` visto en POS y el error de consola en repartidor. Patrón de fan-out de un solo `EventEmitter` en memoria (`packages/api/src/sse-broadcaster.ts`), sin conexión Postgres ni `setInterval` de query por cliente SSE — el pool de Neon (`db.ts`, `max: 10`) no se toca por conexión SSE, evitando el mismo "SSE pool exhaustion" ya resuelto una vez en el CRM de VÉRTICE. Eventos emitidos en el momento del cambio (`POST /api/orders` → `order.created` a todos los POS; `PUT /api/delivery/assignments/:id/assign` → `order.ready_for_dispatch` dirigido solo al `driverId` asignado, nunca broadcast). Verificado con curl `-N` (streaming real, dos rondas independientes), prueba de carga (10 conexiones SSE simultáneas, `/health` 200 durante y después), y Playwright real (`channel: 'chrome'`) de punta a punta — ver "Auditoría de cierre — FASE 2" abajo para el detalle completo. Commit `61d60a2`.

## Auditoría de cierre — FASE 2 (1-sep-2026)

Mismo requisito explícito del dueño del negocio aplicado en el cierre de Fase 1: barrido completo de todo lo que Fase 2 tocó (S07-S08), en navegador real (Playwright con `channel: 'chrome'`, Chrome real del sistema — el Chromium embebido de Playwright sigue sin funcionar en este macOS 13). Se usaron dos cuentas desechables creadas por SQL directo (`qa-test-s08-staff@example.test`, rol `staff`; `qa-test-s08-driver@example.test`, rol `delivery`), un pedido de prueba y una asignación de despacho de prueba, todos creados por SQL directo (nunca vía checkout real ni tocando datos de clientes reales). Toda la cuenta y los datos de prueba fueron borrados al cerrar la auditoría (verificado con `SELECT` posterior, sin filas restantes) — incluyendo un pedido adicional disparado por el propio Playwright durante la prueba en vivo.

**Metodología para el tiempo real específicamente (no solo carga de pantalla, que es lo que cubrió Fase 1):** con el repartidor ya logueado y con el stream SSE conectado en el navegador, se disparó una asignación real (`PUT /api/delivery/assignments/:id/assign`) desde una sesión de staff en paralelo, y se confirmó visualmente que la alerta apareció en el navegador del repartidor **sin recargar la página** — la prueba de fuego real del patrón de fan-out en memoria. Mismo patrón para POS: con la caja abierta y el SSE conectado, se disparó un pedido (`POST /api/orders`) desde otra sesión y se confirmó que apareció en el drawer "Pedidos entrantes" en vivo.

**Resultado: 6/6 funciones probadas cargan y funcionan sin error en navegador real, incluyendo el ciclo de vida completo de una entrega end-to-end disparado por eventos SSE reales.** No se encontraron bugs nuevos en esta auditoría (a diferencia de Fase 1, que encontró el bug de `credentials` en Comandas).

| Función | Carga sin error | Acción principal funciona | Notas / hallazgos |
|---|---|---|---|
| Repartidor: Mis entregas (Activos/Historial) | ✅ | ✅ estado vacío ("Sin entregas pendientes") correcto antes de la asignación de prueba; tras completarse el ciclo, la entrega desapareció de "Activos" (confirmado en DB: `status = 'delivered'`) | No se hizo click explícito en la pestaña "Historial" para verificarla visualmente tras la entrega — inferido correcto por el `delivered_at` en DB y porque `HistorialTab` filtra directamente del mismo array que ya trae el estado `delivered`. Candidato a verificación visual explícita en una sesión futura si el dueño lo pide. |
| Repartidor: alerta de despacho en vivo (SSE `order.ready_for_dispatch`) | ✅ | ✅ `DispatchAlertOverlay` apareció en el navegador **sin recargar** ~1-2s después del `PUT /api/delivery/assignments/:id/assign` disparado desde otra sesión, con los datos correctos (#37915, $5.000, "Cobrar en puerta $5.000", "Despacho a Domicilio"); "ACEPTAR VIAJE" → `PUT .../status {accepted}` → 200, overlay se cierra, entrega pasa a "Activos" | Prueba de fuego real del patrón de fan-out en memoria — no fue solo curl, fue el navegador recibiendo el push sin ninguna acción del usuario. Confirma también el targeting por `driverId`: la alerta llegó únicamente a la sesión del repartidor correcto. |
| Repartidor: marcar estado (ciclo completo) | ✅ | ✅ `Ya recogí el pedido` → `in_transit` (200); GPS: `watchPosition` disparó `POST /api/delivery/location` con la geolocalización mockeada de Playwright, **fila real insertada en `delivery_location_pings`** (verificado con SELECT: lat/lng exactos, `assignment_id` correcto); `Confirmar cobro en puerta` → `Entrega confirmada` → `delivered` (200), `delivered_at` seteado en DB | Ciclo completo `assigned → accepted → in_transit → delivered` verificado de punta a punta, incluyendo el reporte de ubicación real (no simulado a nivel de request — se dejó correr el `watchPosition` real del navegador). No se probó el flujo de foto de comprobante (POD) ni el botón "Reportar fallo" — fuera del alcance de esta auditoría, ya cubiertos por el endpoint de S07. |
| POS: pedidos entrantes en vivo (SSE `order.created`) | ✅ | ✅ tras `POST /api/orders` disparado desde otra pestaña de la misma sesión, el badge "Web" del TopBar mostró el contador sin recargar y el drawer "Pedidos entrantes" mostró la orden nueva (#89711, $12.345, "Web · hace 2s") al abrirlo | Confirma que el SSE solo se conecta cuando la caja está abierta (`tillReady`), como está diseñado — se verificó abriendo turno + caja de prueba antes de la conexión. |
| Consola sin `ERR_CONNECTION_REFUSED` de `/api/events/*` | ✅ | — | Se capturaron 3 errores de consola en total durante toda la corrida: 2 relacionados a `http://127.0.0.1:9101` (`net::ERR_CONNECTION_REFUSED`) — confirmado en `apps/pos/src/lib/print-service.ts:11` que es el **Print Agent local** (puente ESC/POS a la impresora térmica física), no relacionado con S08 ni con este entorno de prueba (no hay hardware POS real conectado a esta máquina) — y errores 401/404 sueltos sin relación a `/api/events/*`. Cero apariciones de `ERR_CONNECTION_REFUSED` contra `/api/events/pos` o `/api/events/delivery` — el bug original que motivó S08 está resuelto. |
| Prueba de carga SSE | — | ✅ | 10 conexiones SSE simultáneas (5 `/api/events/pos` + 5 `/api/events/delivery`) mantenidas abiertas ~20s cada una; `/health` respondió 200 en las 8 verificaciones hechas durante la ventana de carga y en la verificación final — sin degradación ni timeout. Confirma que el pool de Neon (`max: 10`) no se agota con el patrón de fan-out en memoria. |

**Funciones de Fase 2 NO cubiertas por esta auditoría (reportado, no bloqueante):**
- **Foto de comprobante (POD)** al marcar entrega — el endpoint (`POST /api/delivery/assignments/:id/pod`, S07) no se ejercitó en esta corrida porque el flujo de prueba usó la confirmación sin foto (`canDeliver && !podFile`), camino igualmente válido en el código.
- **"Reportar fallo"** — botón visible, no clickeado en esta auditoría.
- **Pestaña "Perfil"** del repartidor (logout) — no visitada en esta corrida.
- **Panel Despacho** de `apps/pos`/`apps/cerebro` (UI de "Asignar repartidor") — el `assign` que disparó el evento SSE se hizo con un `fetch()` directo desde la consola del navegador (mismo endpoint, misma sesión de staff autenticada), no haciendo click a través del modal `assign-driver-modal.tsx`. El contrato del endpoint ya estaba verificado en S07 vía Playwright con la UI real; esta sesión priorizó verificar el nuevo comportamiento (el push SSE), no repetir la verificación de la UI de asignación.
- **`dispatch-rappi`** — no emite evento SSE por diseño (nunca asigna un `driver_id` interno, ver comentario en el handler) — no aplica a este cierre.

**Oportunidades de optimización encontradas (documentadas, no implementadas):**
1. El SSE de POS solo conecta cuando `tillReady` es verdadero (caja abierta) — es el diseño intencional (`apps/pos/src/app/page.tsx:75`), pero significa que una tablet POS que solo está "mirando" sin caja abierta no recibe alertas de pedidos web. Vale la pena que el dueño confirme si esto es aceptable o si Despacho (`cerebro`, sin gate de caja) debería tener su propio consumidor de `/api/events/pos` para verlas sin necesidad de turno abierto — no se tocó, es una decisión de producto, no un bug.
2. No se probó explícitamente la reconexión (cortar la conexión SSE a mitad de sesión y confirmar que el backoff exponencial del cliente reconecta) — el código de reconexión ya existía antes de S08 en ambos clientes y no se tocó, pero no se ejercitó ese camino específico en esta auditoría por no forzar una caída de red real contra producción.
3. La foto de comprobante (POD) y "Reportar fallo" quedan como pendientes de verificación visual explícita para una futura sesión, aunque ambos endpoints ya estaban cerrados en S07.

---

## FASE 3 — Portal B2B + Tienda Web Cliente Final (S09-11, 48h)

Hoy un cliente real **no puede registrarse ni comprar** en seoulshop.cl. Esto es tan crítico como el panel admin para "entregar el proyecto como debe ser".

**S09 (16h) — Auth de cliente final — ✅ COMPLETA (1-sep-2026):**
`POST /api/customer/register` (sin password en el form — genera una temporal, la envía por correo, `must_change_password=true`, mismo patrón que el onboarding de staff; "reclama" clientes fantasma creados por POS/checkout invitado en vez de duplicarlos, porque `customers.email` es `UNIQUE`), `POST /api/customer/login` (cookie `seul_customer_session`, dominio `.seoulshop.cl` en producción vía Origin, sin prefijo `__Host-` — mismo patrón correcto de Fase 0, no se repitió el bug), `GET /api/customer/me`, `POST /api/customer/logout` (borra la cookie con los mismos atributos con los que se seteó), `POST /api/customer/password-change` (autenticado), `POST /api/customer/password-forgot` + `POST /api/customer/password-reset` (token de un solo uso en `password_reset_tokens`, TTL 1h, nunca revela si el email existe). Todas las tablas de `customer-auth.ts` y las columnas de auth de `customers.ts` ya estaban migradas en producción (confirmado con `information_schema` antes de escribir código) — cero migración nueva necesaria.

**Aislamiento staff↔cliente (instrucción explícita de esta sesión, verificada):** cookie de nombre distinto (`seul_customer_session`, nunca `seul_session`) y firmada con `CUSTOMER_JWT_SECRET` — una clave derivada de `JWT_SECRET` vía HMAC-SHA256 (`db.ts`), no una reutilización directa. Verificado con tokens forjados localmente (secretos dev, sin tocar credenciales de staff reales): un token de cliente devuelve 401 contra `GET /api/auth/users` (staff), y un token con forma de staff devuelve 401 contra `GET /api/customer/me`, con un control positivo confirmando que ese mismo token de staff sí funciona contra `GET /api/auth/me`. `requireCustomerSession(c)` nuevo en `middleware/auth.middleware.ts`, paralelo a `requireSession` de staff, sin mezclarlos.

**Rate limiting:** `checkAndRecordRateLimit` (mismo limitador genérico de S02) aplicado a `customer:register`, `customer:login` (bucket por email) y `customer:password-forgot` (bucket por email, para no poder usarlo para bombardear la bandeja de una víctima) — 20 req/5min. Verificado: 20 intentos de login fallidos devuelven 401, el 21º devuelve 429.

**Preflight OPTIONS de `/api/customer/login`:** reutiliza `loginPreflightHeaders` (mismo patrón exacto de `/api/auth/login` de staff, no uno nuevo) — en la práctica, el middleware CORS global (`app.use('/api/*', corsOptions)`) ya intercepta el OPTIONS antes de llegar a este handler (mismo comportamiento ya presente en el login de staff, confirmado comparando ambos con `curl -X OPTIONS`), y refleja el Origin real de la whitelist sin wildcard — verificado con Origin permitido (`https://seoulshop.cl` reflejado) y Origin no permitido (header `Access-Control-Allow-Origin` ausente, nunca `*`).

**Verificado con curl end-to-end, dos rondas independientes** (local contra Neon de producción antes del push, y otra vez contra `https://api.seoulshop.cl` después del push) con cuentas `qa-customer-s09*@example.test` desechables: registro → login con password temporal → `/me` → cambio de password → login con password vieja (401) → login con password nueva (200, `mustChangePassword:false`) → `password-forgot` (siempre `ok:true`, exista o no la cuenta) → `password-reset` con token real → reutilizar el mismo token (400, ya usado) → login con password reseteada (200) → `logout` (`Set-Cookie` con `Max-Age=0`, mismo dominio). Catálogo público (`/api/products`, `/api/products/meta/categories`) confirmado 200 sin sesión en ambas rondas. `/health` en 200 antes y después del push. Toda la data de prueba (2 cuentas, tokens de reset, email_queue/email_log, rate_limit_events) borrada al cerrar, verificado con `SELECT` posterior sin filas restantes.

**Playwright — BLOQUEADO, no por esta sesión:** se intentó verificar el flujo de registro+login en navegador real (`apps/web` local con `next dev`, apuntando al API local) y se descubrió un bloqueador crítico preexistente — ver el hallazgo 🔴 en la fila "Tienda web: login/registro cliente" de la sección 6. En resumen: `apps/web/app/` (carpeta duplicada en la raíz, fuera de `src/`) le gana a `apps/web/src/app/` en la resolución de rutas de Next.js, así que **ninguna** ruta de `src/app/` — ni las de esta sesión, ni el catálogo de S03, ni nada de `apps/web` — es alcanzable hoy, local o en producción. No se tocó (fuera de alcance de S09, riesgo de deploy no autorizado) — queda como el primer punto a resolver en S10.

Commit: `dc52a74` — pusheado a `main`, auto-deploy Railway confirmado (nuevo deployment ID, `/health` 200, rutas nuevas respondiendo tras el redeploy).

**S10 (16h) — Catálogo + pedidos del cliente — ✅ COMPLETA (1-sep-2026):**

**0. Hallazgo crítico previo a todo lo demás (ver sección 6, filas "Tienda web: login/registro cliente" y "catálogo público"):** el bloqueador de S09 (carpeta `apps/web/app/` duplicada) ya venía resuelto al empezar esta sesión (commits `23aa08f`, `b550c2b`, mismo día, antes de esta sesión). Al verificar la homepage en navegador real se encontró un TERCER bloqueador del mismo síntoma ("/" no muestra la página real): `apps/web/src/middleware.ts`, un archivo preexistente sin relación con los duplicados de `app/`/`page.tsx`, redirigía con 307 cualquier request a `/` → `/productos` — la homepage real en `(shop)/page.tsx` nunca era alcanzable en producción pese a que su código y el build de Vercel eran correctos (confirmado con `vercel inspect --logs`: la ruta `/` compilaba bien, el problema era 100% de runtime, no de build). Eliminado (no tenía otro propósito el archivo) — commit `e85bf37`. Verificado con `curl -I` (307→200) y Playwright real (Chrome): la homepage editorial (hero, categorías, "Lo más pedido", banner de descuento) renderiza correctamente.

**1. Catálogo — detalle público por slug:** `GET /api/products/:slug` — contraparte pública de `/api/products/id/:id` (esa exige sesión staff, sirve a cerebro por id). Mismo criterio de privacidad de precios que `GET /api/products` (S03): nunca expone `costPrice`/`priceB2B`/`pricePOS`/`discountXXXPct`, ni siquiera con sesión de cliente activa (verificado explícitamente — un cliente logueado no es staff). Solo `status='active'` es alcanzable por slug. Consumido por `apps/web/.../producto/[slug]/page.tsx` (`apiServerFetch('/api/products/${slug}')`, ya escrito desde antes, solo esperaba el backend). `GET /api/products` (listado) y `GET /api/products/meta/categories` ya cubrían home + `/productos` sin cambios (S03).

**2. Pedidos del cliente:** `GET /api/customer/orders` — filtra siempre por el `customerId` de `requireCustomerSession`, nunca por parámetro. Consumido por `apps/web/.../cuenta/pedidos/page.tsx` (ya existía desde S09).

**3. Checkout público — el hallazgo más grande de la sesión:** el frontend (`createWebOrder`/`upsertGuestCustomer` en `apps/web/src/lib/api.ts`) llamaba `POST /api/orders/public` y `POST /api/customers/guest` — **ninguno de los dos existía en el backend**. No era "falta vincular customer_id a un endpoint que ya funciona" (como asumía el plan) — el checkout estaba roto end-to-end desde que se escribió el código, en cualquier ambiente, siempre. Se construyeron ambos:
- `POST /api/customers/guest` — upsert de cliente invitado, mismo patrón "reclamar fantasma" que `POST /api/customer/register` (S09): `customers.email` es `UNIQUE`, un email repetido reclama la fila existente en vez de duplicar.
- `POST /api/public/orders` — crea el pedido y sus `order_items`. `customer_id` SIEMPRE viene de la sesión de cliente activa cuando existe (nunca del body, así el cliente logueado lo intente) — sin sesión, viene del body (guest checkout). `unitPrice` del body se IGNORA por completo — se recalcula desde `products.price_retail` en el momento de crear el pedido, para que nadie pueda mandar un total manipulado (verificado con curl mandando `unitPrice:1` en un producto de $1.200 — el pedido quedó en $1.200, no $1). Deliberadamente en un path fuera de `/api/orders*` (ese prefijo exige API key con scope `orders:write` o sesión STAFF vía `requireAuthMiddleware` — un visitante anónimo jamás pasa por ahí) — mover el endpoint de path evita depender de un detalle fràgil de orden de registro de rutas en Hono. `pdfToken` se devuelve `null` — no existe generación de PDF/boleta en este backend todavía (Fase SII/DTE, pospuesta post-entrega por decisión del cliente, commit `042e8f4`); el checkout no usa ese valor hoy. NO descuenta inventario — ningún endpoint del sistema lo hace todavía (deuda preexistente, no introducida aquí).
- `getOptionalCustomerSession` nuevo en `middleware/auth.middleware.ts` — contraparte de `getOptionalSession` (S03) para clientes: nunca falla, permite que el checkout de invitado siga funcionando sin sesión mientras prefiere la sesión sobre el `customerId` del body cuando existe.

**4. Precios — confirmado, sin cambios necesarios:** un cliente logueado (`seul_customer_session`) usa `getOptionalSession` (que solo lee la cookie de staff `seul_session`) internamente en `GET /api/products`/`GET /api/products/:slug` — nunca matchea, así que sigue viendo únicamente `priceRetail`/`priceWeb`, igual que un visitante anónimo. Verificado con curl mandando la cookie de cliente real a ambos endpoints.

**No se tocó ninguno de los endpoints de auth de cliente de S09** (register/login/me/logout/password-*).

**Verificado con curl end-to-end contra producción** (`https://api.seoulshop.cl`, cuentas `qa-test-s10-*@example.test` desechables + 1 producto real): registro → reset de password vía token leído directo de Postgres (sin acceso al correo) → login → `GET /api/customer/orders` vacío → `POST /api/public/orders` con sesión activa y con `customerId`/`unitPrice` deliberadamente falsificados en el body → confirmado en Postgres que el pedido quedó con el `customer_id` real de la sesión y el precio real de catálogo, no los valores falsificados → el pedido aparece en `GET /api/customer/orders` → flujo de invitado (`POST /api/customers/guest` → reclamar el mismo email de nuevo, `isNew:false` → `POST /api/public/orders` sin sesión) → validaciones (modo de entrega inválido, sin `customerId`, producto inexistente, cliente inexistente) → confirmado que `/api/orders*` (staff/API-key) sigue devolviendo 401 a un visitante anónimo, sin regresión. Toda la data de prueba (customers, orders, order_items, password_reset_tokens, email_queue, email_log, rate_limit_events) borrada al cerrar, verificado con `SELECT` posterior sin filas restantes.

**Verificado con Playwright real (Chrome) contra producción:** `/` (homepage editorial tras el fix del middleware), `/productos` (96 tarjetas reales), `/producto/beb-012` (detalle con precio, badges BAES/cadena de frío, botón "Agregar al pedido"), `/cuenta/login`, `/cuenta/pedidos` sin sesión → redirige a `/cuenta/login`, `/checkout` con carrito vacío → estado vacío correcto. **Flujo de checkout de invitado de punta a punta en navegador real**: agregar producto al carrito en `/producto/beb-012` → `/checkout` → llenar nombre/correo de invitado → confirmar pedido → pantalla "¡Pedido #N enviado!" — con la red interceptada confirmando que el navegador real llamó `POST /api/customers/guest` (200) y `POST /api/public/orders` (200) con los paths y payloads correctos. Dato de prueba de este flujo también borrado al cerrar.

**Build de Vercel (`apps/web`, proyecto "web") verificado con `vercel inspect --logs`** en las dos rondas de esta sesión (antes y después del fix del middleware): "Route (app)" incluye `/`, `/productos`, `/producto/[slug]`, `/checkout`, `/cuenta/pedidos` y el resto de rutas de `(shop)`/`(b2b)`/`cuenta`, compilación limpia (`✓ Compiled successfully`), deployment con alias confirmado a `https://seoulshop.cl`.

Commits: `7c9f36a` (backend: `/api/products/:slug`, `/api/customer/orders`, `/api/customers/guest`, `/api/public/orders`, `getOptionalCustomerSession`), `1a85efe` (frontend: `createWebOrder` apunta a `/api/public/orders`), `e85bf37` (elimina `apps/web/src/middleware.ts`) — los tres pusheados a `main`, auto-deploy Railway (API) y Vercel (`web`) confirmados, `/health` 200 antes y después de cada push.

No se avanzó a S11 (Portal B2B) — fuera de alcance de esta sesión.

**S11 (16h) — Portal B2B (empresa mayorista) — ✅ COMPLETA (2-sep-2026), última sesión de Fase 3:**

**0. Auth B2B — decisión de arquitectura (verificada contra el schema antes de escribir código):** el portal B2B reutiliza EXACTAMENTE la misma sesión de cliente que B2C (`seul_customer_session` / `requireCustomerSession`, S09) — no se construyó un login ni una cookie propios. `b2b_companies.customer_id` (`packages/db/src/schema/customers.ts`) es `NOT NULL` y referencia una sola fila de `customers`: el modelo es un dueño/contacto por empresa, no varios usuarios por empresa. Confirmado además porque `apps/web/.../b2b/login/page.tsx` (escrito en una sesión previa, antes de que el backend existiera) ya llamaba `/api/customer/login` + `/api/customer/me` — el diseño ya estaba decidido, esta sesión solo construyó el backend que faltaba. `requireB2BCompany(c)` (nuevo, local a `server.ts`) es el equivalente de "requireB2BSession": llama a `requireCustomerSession` y resuelve la empresa asociada a ese `customerId`; un cliente B2C sin empresa recibe 403 en cualquier endpoint B2B — verificado explícitamente.

**1. Hallazgo de arquitectura antes de escribir rutas:** `app.use('/api/b2b*', requireAuthMiddleware + requireScopeMiddleware(['orders:write']))` ya existía (protegiendo las 3 rutas de `/api/b2b/quotes/*`) y habría bloqueado con 401 cualquier ruta nueva bajo `/api/b2b/*` antes de que sus propios handlers pudieran evaluar la sesión de cliente — Hono compone todos los middlewares cuyo patrón matchea la request, sin importar el orden de los handlers específicos en el archivo. Se estrechó el wildcard a `/api/b2b/quotes*` — cero regresión (las 3 rutas de quotes quedan exactamente igual de protegidas).

**2. Endpoints construidos (7, todos verificados con curl contra Neon de producción y luego contra `https://api.seoulshop.cl` tras el push):**
- `POST /api/b2b/registro` — público, sin sesión. Reclama cliente fantasma / crea uno nuevo con password temporal (mismo patrón que `/api/customer/register`, S09) o, si el email ya es una cuenta B2C activa, solo asocia la empresa sin tocar su password existente. RUT validado con dígito verificador real (`isValidRUT`/`normalizeRUT`, nuevos — el frontend solo formatea, no valida DV). Decisión documentada: la cuenta queda logueable de inmediato (no existe una pantalla en cerebro para "aprobar" el registro de una EMPRESA nueva — solo para solicitudes de CRÉDITO); `status` de la empresa queda en `'pending'` igual, el copy "te contactaremos para activar tu cuenta" se entiende como activar el canal de pedidos por WhatsApp, no el acceso al portal.
- `GET /api/b2b/catalogo` — precios netos B2B, solo empresa autenticada (nunca público, nunca cliente B2C normal — verificado). `apps/web/.../b2b/catalogo/page.tsx` (Server Component) reenvía la cookie de sesión a mano vía `cookies()` de `next/headers`, mismo patrón que `serverFetch` de `apps/cerebro/src/lib/api.ts`.
- `GET /api/b2b/empresa/me` — datos de la empresa de la sesión (incluye `creditPct` calculado).
- `POST /api/b2b/credit-request` — `companyId` siempre de la sesión, nunca del body (verificado: un `companyId` falsificado en el body es ignorado).
- `GET /api/b2b/credit-requests/:id` — detalle, accesible por la empresa dueña o por staff owner/admin.
- `PATCH /api/b2b/credit-requests/:id/review` — staff owner/admin. No estaba en la lista original del plan bajo ese nombre exacto, pero es el endpoint real que `apps/cerebro/.../b2b/solicitudes/page.tsx` (ya construido antes de esta sesión) llama para los botones Aprobar/Rechazar — sin esto, la lista de solicitudes no tendría ninguna acción posible. Al aprobar, acredita el monto en `b2b_wallet_ledger` + `b2b_companies.wallet_balance_clp` (único lugar del sistema que escribe en el ledger hoy). Una solicitud ya revisada no se puede revisar de nuevo (409, verificado).
- `GET /api/b2b/solicitudes` — staff owner/admin. Pese al nombre, es el listado de solicitudes de CRÉDITO (`b2b_credit_requests`), no de registro de empresa — confirmado leyendo `apps/cerebro/.../b2b/solicitudes/page.tsx` antes de escribir la ruta.
- `GET /api/b2b/pedidos/:id` y `GET /api/b2b/wallet/:id` — `:id` es el id de la EMPRESA; se verifica contra la empresa de la sesión (403 si no coincide, verificado con un id ajeno). Los pedidos B2B no tienen columna propia en `orders` (no existe `orders.company_id`) — se resuelven por `customer_id = company.customer_id AND channel = 'b2b'`.

**3. Fix de seguridad/UX en 3 pantallas ya construidas (`apps/web/.../b2b/credito`, `.../estado-cuenta`, `.../catalogo`):** antes de esta sesión, `credito` y `estado-cuenta` dejaban que cualquier visitante tecleara un UUID de empresa a mano en un input de texto, sin `credentials: 'include'` — ni protegían datos ajenos ni podían funcionar contra el backend nuevo (que ignora `companyId` del body y verifica el `:id` contra la sesión). `catalogo` corría sin reenviar la cookie de sesión, lo que habría mostrado precios netos a cualquier visitante anónimo — contrario al requisito explícito de esta sesión. Las 3 pantallas ahora resuelven la empresa siempre de la sesión activa (mismo patrón que `dashboard.tsx`, que ya lo hacía bien). `catalogo` muestra un estado bloqueado con CTA a login/registro cuando no hay sesión de empresa, en vez de una tabla de precios pública o un crash.

**4. Verificación con curl end-to-end** (Neon de producción local primero, luego `https://api.seoulshop.cl` tras el push) con cuentas `qa-test-s11-*@example.test` desechables: registro → login con password temporal → `empresa/me` → catálogo con precios B2B reales → catálogo sin sesión (401) → cliente B2C normal registrado aparte intentando `catalogo`/`empresa/me` (403 en ambos, confirmado que nunca ve `priceB2B` ni siquiera en `GET /api/products`) → `credit-request` con `companyId` falsificado en el body (ignorado, confirmado en Postgres que quedó con el id real de la sesión) → `GET /api/b2b/solicitudes` (staff) → `PATCH .../review` aprobar (wallet +monto, ledger creado) → re-revisar (409) → rechazar la otra solicitud → `pedidos/:id` con un pedido `channel='b2b'` real insertado por SQL (aparece correctamente) → `wallet/:id`/`pedidos/:id` con un id ajeno (403 en ambos) → ruta legacy `/api/b2b/quotes` sigue devolviendo 401 sin sesión staff (cero regresión del estrechado de middleware).

**5. Verificado con Playwright real (`channel: 'chrome'`) contra producción, cuenta `qa-test-s11-pw@example.test` desechable:** `/b2b/registro` (flujo de 2 pasos → "¡Solicitud enviada!") → login con la password temporal leída de `email_queue` (sin acceso a correo real) → redirige a `/b2b/dashboard` mostrando la razón social real → `/b2b/catalogo` autenticado muestra la tabla de 77 productos reales con precios mayoristas (`$900` B2B vs `$1.200` retail, `-25%`) → `/b2b/credito` envía solicitud → `/b2b/estado-cuenta` carga los datos de la empresa automáticamente, sin pedir ningún ID → un contexto de navegador SIN sesión visitando `/b2b/catalogo` ve el estado bloqueado ("Catálogo exclusivo para empresas mayoristas") y NUNCA la tabla de precios. `vercel inspect --logs` confirmó build limpio con las 8 rutas `/b2b/*` en la tabla "Route (app)", `/b2b/catalogo` correctamente marcada dinámica (ƒ, antes era estática) por leer la cookie por request.

**6. Datos de prueba** (3 empresas, 3 clientes B2B + 1 B2C, 1 usuario staff owner, 1 pedido, credit requests, wallet ledger) creados y borrados en la misma sesión — verificado con `SELECT` posterior sin filas restantes. `/health` en 200 antes y después de cada push.

Commits: `210545d` (backend: 7 endpoints + narrow de middleware + `isValidRUT`), `95abfc0` (frontend: `credito`/`estado-cuenta`/`catalogo` cableados a sesión) — ambos pusheados a `main`, auto-deploy Railway y Vercel confirmados.

---

## Auditoría de cierre — FASE 3 (2-sep-2026)

Mismo requisito explícito del dueño del negocio aplicado en Fase 1 y Fase 2: barrido completo de todo lo que Fase 3 tocó (S09-S11), en navegador real (Playwright, `channel: 'chrome'` — el Chromium embebido sigue sin funcionar en este entorno), con selectores inequívocos (`getByRole`/`getByPlaceholder`/texto único, nunca substring ambiguo — lección explícita del incidente de Comandas en el cierre de Fase 1). Cuenta B2C desechable (`qa-test-fase3-close@example.test`) y cuenta B2B desechable (`qa-test-s11-pw@example.test`, reutilizada de S11), creadas por SQL directo / vía flujo real, borradas al cerrar.

**Resultado: 3/3 flujos NUEVOS de S11 (Portal B2B) verificados con Playwright real de punta a punta, sin errores; los flujos de S09-S10 confirmados sin regresión (curl 200 en sus rutas, cero cambio de código que pudiera afectarlos).** No se encontraron bugs nuevos en el barrido de cierre — los 3 bloqueadores de routing de `apps/web` (carpeta `app/` duplicada, `page.tsx` duplicado, `middleware.ts` que redirigía "/") ya se habían resuelto en S09/S10 y se confirmaron aquí como definitivamente cerrados (`/` sigue devolviendo la homepage editorial real, no un redirect).

Alcance de esta pasada: Playwright real de punta a punta en los flujos NUEVOS de S11 (Portal B2B, nunca antes probados en navegador); para los flujos de S09-S10 (ya verificados con Playwright en su propia sesión de cierre) esta auditoría se limita a confirmar con `curl -I` que sus rutas siguen en 200 después de los cambios de S11 — no se repitió el click-through completo porque S11 no tocó ese código (`requireCustomerSession`, checkout, catálogo público) y no hay señal de regresión.

| Flujo | Verificación en esta auditoría | Resultado | Notas |
|---|---|---|---|
| Catálogo público (`/`, `/productos`, `/producto/[slug]`) | `curl -I` (ruta) | ✅ 200 | Funcionalidad completa ya verificada con Playwright en S10 — sin cambios de código en S11. |
| Registro/login cliente (`/cuenta/registro`, `/cuenta/login`, `/cuenta/pedidos`, `/cuenta/dashboard`, `/cuenta/perfil`, `/cuenta/recuperar`) | `curl -I` (ruta) | ✅ 200 | Funcionalidad completa ya verificada con Playwright en S09-S10 — sin cambios de código en S11. |
| Checkout invitado y con cliente logueado (`/checkout`) | `curl -I` (ruta) | ✅ 200 | Funcionalidad completa ya verificada con Playwright en S10 — sin cambios de código en S11. |
| Portal B2B — registro + login | Playwright real, flujo completo | ✅ | `/b2b/registro` (flujo 2 pasos) → "¡Solicitud enviada!" → login con password temporal (leído de `email_queue`, sin acceso a correo real) → redirige a `/b2b/dashboard` mostrando la razón social real. Nuevo en S11 — primera vez que este flujo corre en navegador real. |
| Portal B2B — catálogo + crédito + estado de cuenta | Playwright real, flujo completo | ✅ | Catálogo autenticado: 77 productos con precios mayoristas reales (`$900` B2B vs `$1.200` retail, `-25%`). Solicitud de crédito enviada. Estado de cuenta carga automático (ya no pide UUID a mano, fix de esta sesión). |
| Portal B2B — aislamiento (anónimo + cliente B2C normal) | Playwright (anónimo) + curl (cliente B2C con sesión real) | ✅ | Un navegador sin sesión ve el estado bloqueado en `/b2b/catalogo` ("Catálogo exclusivo…"), nunca la tabla de precios. Un cliente B2C normal (sesión válida, sin empresa) recibe 403 en `/api/b2b/catalogo` y `/api/b2b/empresa/me`, y `GET /api/products` nunca le expone `priceB2B` — probado activamente, no solo asumido. |

**Todas las rutas del build log de Vercel de hoy verificadas con `curl -I` (200 esperado, `/b2b` es 307 por diseño — `redirect('/b2b/catalogo')` en `page.tsx`):** `/`, `/productos`, `/producto/beb-012`, `/checkout`, `/cuenta/login`, `/cuenta/registro`, `/cuenta/pedidos`, `/cuenta/dashboard`, `/cuenta/perfil`, `/cuenta/recuperar`, `/cuenta/recuperar/[token]`, `/cuenta/cambiar-clave`, `/devoluciones`, `/faq`, `/privacidad`, `/terminos`, `/b2b/catalogo`, `/b2b/login`, `/b2b/registro`, `/b2b/dashboard`, `/b2b/credito`, `/b2b/estado-cuenta`, `/b2b/postventa` — **0 rutas con 404/500.**

**Deuda/gaps encontrados en esta auditoría (documentados, no bloqueantes, fuera de alcance de Fase 3):**
1. **No existe pantalla en cerebro para aprobar el REGISTRO de una empresa nueva** (`b2b_companies.status` queda en `'pending'` para siempre salvo un `UPDATE` manual en la base) — solo existe aprobación de solicitudes de CRÉDITO (`/b2b/solicitudes`, ya construida). Decisión documentada en S11: el acceso al portal no está gateado por esto (la cuenta es logueable de inmediato tras el registro), así que no bloquea el uso real, pero el dueño no tiene hoy un botón para pasar una empresa de `pending` a `approved`. Candidato claro para una sesión futura (Fase 4/5 o un hotfix corto).
2. **`b2b_companies.credit_used_clp` no lo escribe ningún endpoint todavía** — solo `wallet_balance_clp` se mueve (vía el ledger de S11). `creditLimitClp`/`creditUsedClp` (línea de crédito con días de pago) y `walletBalanceClp` (saldo prepago) son conceptos distintos en el schema; hoy solo el segundo tiene flujo de escritura completo. No bloqueante para v1.0 (el dueño no pidió facturación a crédito con plazo todavía), documentado para cuando se aborde.
3. **Sin UI de "hacer un pedido" dentro del portal B2B** — el catálogo (`/b2b/catalogo`) es de solo lectura; el flujo real de compra sigue siendo por WhatsApp (mensaje explícito en la página: "Para hacer un pedido mayorista… escríbenos"). Es una decisión de producto ya reflejada en el frontend existente antes de esta sesión, no un bug — pero vale la pena que el dueño confirme si v1.1 debería tener carrito B2B real contra `POST /api/orders` (channel `b2b`, ya soportado por el enum) en vez de WhatsApp.
4. Los 3 gaps ya documentados en el cierre de Fase 1 (Clientes CRM interno, Seguridad `GET /api/tienda-config` sin key, labels de UX) siguen sin tocar — no eran parte del alcance de Fase 3.

**Oportunidades de optimización encontradas (documentadas, no implementadas):**
1. `POST /api/b2b/registro` no valida si el RUT de la empresa corresponde a una razón social real (no hay integración con el SII/registro de empresas) — validación es solo dígito verificador matemático. Aceptable para v1.0 (mismo nivel de validación que el resto del sistema), documentado por si el dueño quiere subir el rigor más adelante.
2. El límite de crédito default (`$500.000`, tier `hoobae`) es fijo en el `INSERT` de `b2b_companies` — no hay UI para que el dueño configure estos defaults por tienda_config. No urgente para v1.0.

---

## Adición post-Fase 3 — Nota de Venta + seam DTE (2-sep-2026)

Fuera de la numeración S01-S17 — tarea puntual pedida por el dueño, mismo patrón que "Comandas en POS" (ver arriba). Contexto: la conexión real al SII (S12) sigue pospuesta post-entrega (decisión de negocio, ver línea 370 de este plan) — pero imprimir un comprobante en cada venta es funcionalidad crítica desde el día uno, así que hoy se emite como **Nota de Venta** (documento no tributario, legal de emitir sin timbre SII), con el sistema arquitectónicamente listo para "enchufar" un proveedor DTE real más adelante sin rediseñar nada.

**Estado real encontrado (antes de tocar nada):** `POST /api/orders` — el endpoint que `apps/pos/.../checkout-shell.tsx` (la UI de cobro real, montada en `apps/pos/src/app/page.tsx`) llama al confirmar una venta — era un stub heredado de un intento de pedido web (`customer_email`/`items`/`total` planos) que el checkout de POS nunca llamó con ese shape. **Toda venta en caja fallaba con 400 "Missing fields" desde antes de esta sesión** — el checkout de POS nunca funcionó de punta a punta en producción. Se confirmó además que `packages/dte` (MockDTEProvider + OpenFactura + factory, con la interfaz exacta que este plan preveía para el seam) ya existía completo desde antes pero no estaba importado por nadie (`grep` de `@seul/dte` en todo el repo: cero resultados) — arquitectura ya lista, solo desconectada.

También se confirmó cuál motor de render está realmente en la ruta caliente: `packages/pdf-templates/src/html/pos-receipt.ts` (HTML "quiet luxury", vía `print-service.ts` → agente ESC/POS local en `127.0.0.1:9101` o popup HTML de fallback). `boleta-80mm.ts`/`nota-de-venta.ts` (PDFKit) y `apps/pos/src/app/print/page.tsx` (una tercera implementación standalone con su propio HTML inline) son código muerto — cero callers en todo el repo, no se tocaron.

**Cambios (4 commits, un grupo lógico por commit):**

1. **`d2945dd`** — Reescribe `POST /api/orders` (`packages/api/src/server.ts`) para el payload real de POS: items con `productId`/`quantity`/`unitPrice`/`isBaes`, `payments[]`, `shiftId`/`tillSessionId`, `receiver` opcional. Inserta `orders`+`order_items`+`order_payments` en una transacción (número de pedido serializado con `pg_advisory_xact_lock`, ya que `orders.number` no tiene `UNIQUE`). `dteType` se fuerza a `'nota_venta'` en el servidor sin importar lo que envíe el cliente (defensa en profundidad). Llama a `@seul/dte` (`emitDte`) forzado a `DTE_PROVIDER:'mock'` explícito — nunca lee env vars, así que aunque alguien configure `DTE_API_KEY` en Railway antes de tiempo, jamás contacta un proveedor real — y registra el intento en `dte_events` con `provider:'mock'`. Agrega `GET /api/orders/:id/dte-status`, que `checkout-shell.tsx` ya pollea pero no existía.
2. **`2d317b4`** — Gatea Boleta/Factura en `checkout-shell.tsx`: default cambia de `'boleta'` (que nunca funcionó) a `'nota_venta'`; los otros dos botones quedan visualmente deshabilitados (opacidad, tooltip explicando por qué) en vez de ocultos, para que el layout de 3 columnas quede listo el día que se conecte un proveedor real.
3. **`6ec1f64`** — `pos-receipt.ts`: agrega el aviso "No es documento tributario" también justo bajo el título del documento (antes solo aparecía una vez, en el pie) — visible pero de tono neutro (misma clase `.dte-badge`, 9px itálica gris, no alarmante). Revisión UI/UX de los otros 3 templates HTML (`comanda.ts`, `z-report.ts`, `delivery-payout.ts`): comanda y z-report confirmados en la ruta caliente vía grep de sus callers reales, con jerarquía tipográfica ya sobresaliente (número de pedido a 48px/900 en comanda, legible a 2m) — sin cambios necesarios. `delivery-payout.ts` (`renderDeliveryPayoutHtml`) resultó ser código muerto — cero callers en todo `apps/`, la feature de liquidación de turno del repartidor nunca llegó a enchufarlo; diseño ya listo, no se tocó (conectar esa pantalla es una feature aparte, fuera de alcance de impresión POS).
4. **`7358fb8`** — CRÍTICO: el push del commit 1 (nueva dependencia `@seul/dte` en `packages/api`) tumbó producción (`api.seoulshop.cl` → 502 "Crashed", confirmado con `railway logs`: `ERR_MODULE_NOT_FOUND` para `@seul/dte`) porque la etapa runtime del `Dockerfile` solo copiaba `packages/api`+`packages/db` explícitamente — el symlink de pnpm en `node_modules` apuntaba a un directorio que no existía en la imagen final. Fix: agregar `packages/dte` y `packages/pdf-templates` al mismo patrón `COPY --from=builder`. Confirmado con `railway status` → `Online` y `/health` → 200 tras el redeploy.

**Bug encontrado y arreglado durante la verificación (antes del commit 1):** `dte_events.request_payload`/`response_payload` y `order_payments.meta` (columnas `jsonb`) se insertaban con `JSON.stringify(obj)` — postgres.js serializa objetos JS a `jsonb` automáticamente, así que pasarle un string ya serializado producía doble-encode (un string escapado dentro del `jsonb`, no un objeto real). Corregido pasando el objeto JS directo, sin `JSON.stringify` ni `::jsonb`. Verificado con `jsonb_typeof(...) = 'object'` antes de cada commit.

**Verificación end-to-end:** contra la Neon real (misma `DATABASE_URL` de producción), servidor local (`tsx watch`) con JWT firmado localmente (rol `owner`, mismo `JWT_SECRET` de `.dev.vars`): `POST /api/orders` con payload real de POS → 200, `orders`+`order_items`+`order_payments`+`dte_events` correctos por `SELECT` directo (incluyendo `jsonb_typeof = object` en las 3 columnas jsonb), `dteType:'boleta'` enviado deliberadamente en el body y confirmado forzado a `nota_venta` en la fila insertada, matemática BAES correcta, `GET /api/orders/:id/dte-status` responde `issued`, cero líneas de log mencionando Haulmer/OpenFactura (ningún intento de red real). HTML de `pos-receipt.ts` renderizado con datos de prueba fuera de la BD para confirmar el aviso nuevo y que nada se rompió. Todos los pedidos/eventos de prueba borrados al cerrar cada ronda, verificado con `SELECT` posterior (vuelta a 11 `orders`/0 `dte_events` en cada caso). `/health` en 200 y `railway status` en `Online` confirmados después del último push.

**No verificado con una venta real en el navegador (Playwright) por falta de credenciales vigentes:** las credenciales de `CREDENCIALES_OPERACIONALES_2026-08-30.md` (`founder@seoulshop.cl` / `Seoul2025!Founder`) devolvieron `Invalid credentials` contra producción al intentar un login real — parecen haber rotado desde que se escribió ese documento (consistente con los varios incidentes de login documentados en sesiones anteriores). La verificación de arriba cubre el contrato completo del backend contra datos reales, pero falta el click-through real en `pos.seoulshop.cl` — pendiente para cuando el dueño confirme credenciales vigentes o se resetee una cuenta de prueba.

**Deuda/gaps encontrados, no bloqueantes, fuera de alcance de esta sesión:**
1. Las credenciales de staff documentadas no funcionan contra producción — necesita verificación/reset antes de poder hacer QA de click-through real del POS.
2. `assignmentId` — `apps/pos/src/app/page.tsx` (`handleConfirm`) ya lee `data.assignmentId` de la respuesta de `POST /api/orders` para disparar la comanda de cocina + asignación de repartidor en ventas delivery, pero el endpoint nunca lo devolvió (ni antes ni después de este fix) — la creación de la asignación de despacho para pedidos POS con delivery es una feature separada, no tocada aquí.
3. `STORE_INFO` (`packages/pdf-templates/src/constants.ts`) sigue con placeholders (`address: 'CONFIRMAR CON DUEÑO'`, `rut: 'XX.XXX.XXX-X'`, `phone: '+56 9 XXXX XXXX'`) — ya estaba centralizado en un solo lugar antes de esta sesión, no se inventó ningún dato real. Pendiente que el dueño confirme dirección/RUT/teléfono reales.

---

## FASE 4 — Cumplimiento Legal + Analítica + Extras (S12-14, 44h)

**S12 (16h) — Boleta electrónica (SII/DTE) — ⏸️ POSPUESTO POST-ENTREGA (decisión del cliente, 1-sep-2026):** el cliente confirmó explícitamente que la integración SII/DTE (y la elección de proveedor: Haulmer/OpenFactura/SimpleAPI, etc.) se hace DESPUÉS de la entrega de v1.0, no antes. **No bloquea el resto de Fase 4** — saltar directo a S13-S14 cuando se llegue a esta fase. Retomar S12 en una sesión futura post-entrega cuando el cliente decida proveedor.

**S13 (14h) — Seguridad/Legal + Devoluciones — ✅ completo (2-sep-2026):** `GET /api/arcop` (Ley 21.719 — derechos ARCOP), `GET /api/returns`.

- Ambos tenían tabla lista en el schema (`arcop_requests` en `customers.ts`, `returns.ts`) y ya existían en la DB de producción (confirmado con `information_schema` antes de escribir código). `apps/cerebro/.../seguridad/page.tsx` YA esperaba el shape exacto documentado en el brief (`{ requests: [...] }` / `{ returns: [...] }`, `arcopPendientes`/`arcopUrgentes` con umbral ≤3 días).
- **Hallazgo por grep antes de construir (lección explícita de hoy):** `apps/web` ya tenía `/privacidad` y `/devoluciones` construidos con formularios completos apuntando a `POST /api/arcop` y `POST /api/returns` — ninguno de los dos existía. La Ley 21.719 exige que cualquier persona pueda ejercer sus derechos ARCOP tenga o no cuenta, así que ambos POST son públicos (sin `requireSession` ni `requireCustomerSession`), con rate limit (`arcop:create` 10/5min, `returns:create` 20/5min, mismo patrón que el resto del sistema).
- `POST /api/arcop`: valida tipo/nombre/email/notas, enlaza `customerId` si el email coincide con un cliente existente (opcional), calcula `deadline` a 15 días hábiles (`addBusinessDays`, salta sáb/dom — feriados CL no tabulados, deuda menor documentada en el código), notifica al solicitante y a `ADMIN_EMAIL`.
- `POST /api/returns`: valida `orderId` contra `orders` real (404 si no existe, 400 si UUID malformado — código Postgres `22P02`), notifica a `ADMIN_EMAIL`. Deliberadamente sin cuenta de cliente: hoy el checkout emite Nota de Venta, no boleta SII real (S12 pospuesto), así que el `orderId` es la referencia confiable, igual que el flujo real de post-venta por WhatsApp.
- `GET /api/arcop` y `GET /api/returns` (roles `owner`/`admin`, matriz 6.1): devuelven todas las solicitudes/devoluciones, `?status=` filtra en `returns`.
- **Migración 0017** (auto-run en `runMigrationsIfNeeded`, mismo patrón que 0014-0016): `arcop_requests.name`/`.email` — la tabla original solo tenía `customer_id` opcional, sin campo para el nombre/correo de una persona sin cuenta. Verificado en el log de arranque local contra la DB real: `🔄 Running migration 0017... ✅ Migration 0017 applied`.
- **Bug de frontend encontrado y arreglado (no en el brief original, pero bloqueante para verificar "ya no muestra Error al cargar"):** `seguridad/page.tsx` llamaba `fetch()` directo a la API sin reenviar la cookie `seul_session` (Server Component, no usaba el `serverFetch` con `cookies()` de `apps/cerebro/src/lib/api.ts` que sí usan Dashboard/Inventario/Clientes). Con `GET /api/arcop`/`GET /api/returns` ahora exigiendo `requireSession`, esa llamada habría devuelto 401 silencioso (atrapado por el `catch → []` de la página) — mismo síntoma de "Error al cargar"/listas vacías que las auditorías previas ya habían anotado. Fix: `getARCOP`/`getReturns` movidos a `lib/api.ts` con el patrón `serverFetch` estándar.
- **Verificado end-to-end contra la DB real de producción** con cuenta de prueba desechable (`qa-s13-test@example.com`, insertada directo por script porque no hay self-registro de staff, login real, token Bearer): `GET /api/arcop` y `GET /api/returns` → 401 sin sesión, 200 con sesión admin y datos correctos; `POST /api/arcop` → solicitud creada con `deadline` correcto (15 días hábiles desde 2-sep-2026 → 23-sep-2026); `POST /api/returns` → 404 con orderId inexistente, 400 con UUID malformado, 200 con orderId real (`Prueba QA sesion S13, borrar despues.`). Todos los registros de prueba (arcop_requests, returns, email_queue, users) borrados al cerrar — confirmado con `DELETE ... RETURNING` (1 fila cada uno).
- `/health` verificado 200 antes y después del push (deploy Railway).
- **Fuera de alcance, no construido (consistente con "no inventar flujo nuevo" — nada en el frontend ya construido lo pide):** no hay endpoint de aprobar/resolver ARCOP ni de aprobar/procesar una devolución — `seguridad/page.tsx` es de solo lectura hoy (sin botones de acción salvo el PIN maestro). Si el dueño quiere poder resolver desde el panel, es una sesión futura pequeña (`PATCH /api/arcop/:id`, `PATCH /api/returns/:id`, mismo patrón que `PATCH /api/b2b/credit-requests/:id/review`).

**S14 (14h) — Analítica + Extras — ✅ completo (2-sep-2026), última sesión de Fase 4:** `POST /api/analytics/pin-check`, `GET /api/analytics/sales`, `GET /api/faq`, más evaluación (sin construir) de `promotions.ts`/`loyalty.ts`.

**1. Analítica — hallazgo por grep antes de construir:** el único consumidor real de `analytics/*` en las 4 apps es `apps/pos/src/components/pos/sales-history-panel.tsx` — un drawer "Ventas de caja" dentro del POS. No existe ninguna pantalla "Reportes" en `cerebro` (grep confirmó cero resultados) pese a que la matriz de roles de la sección 6.1 la menciona para `viewer` — construir esa pantalla nueva no estaba en el alcance de esta sesión (ningún frontend la espera todavía, hubiera sido inventar flujo). Se confirmó además que el PIN de analítica (`tienda_config.analytics_pin`) es un secreto **distinto** de `void_pin` (el PIN maestro que autoriza anular una venta, `pin-maestro-section.tsx`/S13) — dos PINs de 4 dígitos con propósitos distintos, ambos ya con un valor de prueba guardado desde una sesión temprana del mismo día.

- `POST /api/analytics/pin-check`: `requireSession(c, ['owner','admin','staff'])` + rate limit propio (`analytics:pin-check`, 10/5min por usuario — con solo 10.000 combinaciones de 4 dígitos, es la única defensa real contra fuerza bruta desde una sesión de cajero ya válida). Compara `pin` del body contra `tienda_config.analytics_pin`; sin PIN configurado, deniega por defecto (nunca un fallback hardcodeado tipo `'abcd'` — ver el bug de `getVoidPin` corregido en el punto 3).
- `GET /api/analytics/sales?tillSessionId=`: mismos agregados que `computeTillZReport` (Turnos/Z-report, S06) — KPIs (`totalRevenue`, `orderCount`, `avgTicket`, `byMethod`) excluyen órdenes `status='cancelada'`, pero el listado de `orders` SÍ las incluye (con `voidedAt`/`voidReason`) para que el cajero vea qué se anuló en su turno. `tillSessionId` es obligatorio (400 si falta) — nunca "todas las ventas de la tienda" sin acotar, siguiendo el único contrato que el frontend ya tiene escrito.

**2. FAQ pública — hallazgo por grep antes de construir:** `apps/web/.../faq/page.tsx` ya llamaba `GET /api/faq` con fallback silencioso a un `STATIC_FAQ` hardcodeado (12 preguntas) si el endpoint fallaba o devolvía 0 entries — así que la ausencia del endpoint nunca se veía como error en producción, solo como contenido no editable. La tabla `faq_entries` (`packages/db/src/schema/faq.ts`) **ya existía en la DB de producción, vacía** (confirmado con `information_schema` antes de escribir código — otro caso, igual que `promotions`/`loyalty_ledger` del punto 4, de un `drizzle push` completo del schema hecho en algún momento sin que nadie construyera las rutas). Migración 0018 la siembra con el mismo contenido que `STATIC_FAQ` la primera vez que arranca con la tabla en 0 filas, para que el dueño tenga algo real y editable desde el día uno (`GET /api/faq`, público, sin sesión — matches el `fetch()` sin `credentials` que la página ya hace).

**3. Bug corregido — `getVoidPin()` (deuda documentada desde el cierre de Fase 1/S06):** `seguridad/page.tsx` llamaba `fetch()` directo a `${API}/api/tienda-config` (**sin** `:key`, sin reenviar la cookie de sesión) — no coincide con la ruta real (`GET /api/tienda-config/:key`, que además exige `requireSession`), así que siempre fallaba silenciosamente y la página mostraba el PIN por defecto hardcodeado `'abcd'` en vez del PIN maestro real guardado por `PinMaestroSection`. Se confirmó leyendo el código consumidor que este PIN (`void_pin`) es distinto del PIN de analítica del punto 1 — autoriza anulaciones de venta en POS (`POST /api/orders/:id/void`, ver hallazgo del punto 5), no el acceso al historial de ventas. Fix: `getVoidPin()` movido a `lib/api.ts` con el mismo patrón `serverFetch` que `getARCOP`/`getReturns` (S13), apuntando a `GET /api/tienda-config/void_pin`. Es un fix trivial (mueve una función, no cambia lógica de negocio) dentro de la misma pantalla de Seguridad que S13 ya tocó — no se consideró fuera de los límites estrictos de esta sesión.

**4. Evaluación de `promotions.ts`/`loyalty.ts` — recomendación: postergar ambos a v1.1.** No se construyó ninguna ruta ni UI, según lo pedido. Hallazgos que sustentan la recomendación:

- **Las 4 tablas relevantes (`promotions`, `promotion_redemptions`, `loyalty_ledger`, y `faq_entries` del punto 2) ya existen en la DB de producción**, con columnas idénticas a `packages/db/src/schema/{promotions,loyalty}.ts` — confirmado con `information_schema` — pero **vacías** y sin ningún endpoint que las lea o escriba. El trabajo de schema está 100% listo; lo que falta es enteramente diseño de producto + API + UI.
- **Promociones: cero superficie de consumo en todo el repo.** `apps/web/src/lib/api-server.ts` reserva una clave de cache-tag `promotions: 'promotions'` que ningún componente usa — ni siquiera un placeholder visual. El checkout (`apps/web/.../checkout/page.tsx`) no tiene campo de código de descuento. Construir esto en v1.0 significaría diseñar la UI de principio a cero (campo de código en checkout, validación de vigencia/mínimo/tope de usos, aplicación del descuento al total), no solo conectar un backend a un frontend que ya lo espera — un patrón muy distinto al resto de Fase 1-4, donde casi siempre el frontend ya existía esperando el endpoint.
- **Fidelización tiene MÁS superficie de frontend de la documentada hasta ahora, pero bloqueada por un gap más grande y anterior:** `apps/cerebro/.../clientes/[id]/page.tsx` y `clientes/page.tsx` (construidos por una sesión concurrente sin numeración formal, fuera del alcance S01-S13 documentado) ya leen y muestran `customer.loyaltyBalance` y un ledger de lealtad vía `getCustomerTimeline()`. Pero **ni `loyaltyBalance` es una columna real de `customers`** (no existe en el schema — tendría que calcularse en vivo con `SUM(points) FROM loyalty_ledger`) **ni existe ningún endpoint `/api/customers*` en el backend** (`GET /api/customers`, `/:id`, `/:id/timeline`, `/search` — los cuatro llamados por ese frontend no están registrados en `server.ts`, confirmado por grep). Es decir: la fidelización no es el gap más urgente ahí — el módulo completo de Clientes/CRM interno (ya señalado como deuda desde el cierre de Fase 1) es el prerrequisito real. Construir loyalty sin ese CRM primero dejaría puntos que nadie en `cerebro` puede ver ni ajustar manualmente.
- Ninguna orden hoy genera puntos automáticamente (no hay hook de "earn" en `POST /api/orders`) ni hay UI de canje en el checkout — ambos extremos (ganar y gastar puntos) faltan, no solo uno.
- **Recomendación como arquitecto:** postergar promociones Y fidelización a v1.1. Ninguna de las dos es un bloqueador legal ni operacional para abrir la tienda (a diferencia de SII/DTE, S12) — son features de crecimiento/retención, no de operación día 1. Si el dueño quiere adelantar una de las dos, fidelización es la de menor esfuerzo incremental *después* de que exista el módulo Clientes/CRM (que de todos modos hay que construir para que "Clientes" dentro de `cerebro` deje de ser un placeholder) — promociones requiere trabajo de UI de checkout que no tiene ningún punto de partida hoy. **Esta es una recomendación, no una decisión — queda pendiente de que el dueño de Seoul Kims confirme si alguna entra en v1.0 o ambas quedan para v1.1.**

**5. Hallazgo colateral, no construido (fuera de alcance — dominio POS/checkout, protegido por los límites de esta sesión):** `sales-history-panel.tsx` también llama `POST /api/orders/:id/void`, que **no existe** en `server.ts` (confirmado por grep) pese a que la tabla `pos_void_events` (auditoría de anulaciones) ya está modelada en `packages/db/src/schema/pos-void-events.ts`. Hoy un cajero puede ver el historial de ventas (con el fix de esta sesión) pero no puede anular una venta desde ahí — el botón "Anular venta" y su modal de confirmación (PIN + motivo) existen en el frontend pero el POST al que apuntan siempre devolverá 404. Es un gap real y pre-existente a esta sesión, no introducido por ella — candidato claro para una sesión de hotfix corta (mismo patrón que `PATCH /api/b2b/credit-requests/:id/review`: validar PIN contra `void_pin`, marcar `orders.status='cancelada'` + `voided_by`/`voided_at`/`void_reason`, insertar en `pos_void_events`).

**Verificado con curl end-to-end contra la DB real de producción** (servidor local con `.dev.vars` apuntando a la misma Neon), con un usuario de prueba desechable (`qa-s14-test@example.test`, insertado directo por script) y un turno + caja + 2 pedidos de prueba (uno normal, uno con `status='cancelada'`) igualmente desechables: `POST /api/analytics/pin-check` → 401 sin sesión, 403 rol `delivery`, `{ok:false}` con PIN incorrecto, `{ok:true}` con el PIN real, `429` en el 11º intento (rate limit); `GET /api/analytics/sales` → 401 sin sesión, 403 rol `delivery`, 400 sin `tillSessionId`, 200 con KPIs correctos (`totalRevenue`/`orderCount`/`byMethod` excluyendo la orden cancelada, que sí aparece en el array `orders` con su `voidReason`); `GET /api/tienda-config/void_pin` (fix del punto 3) devuelve el valor real guardado. Repetido contra `https://api.seoulshop.cl` tras el push, con un segundo usuario desechable creado vía login real (no token forjado) para confirmar que el `JWT_SECRET` de Railway firma/verifica igual que el flujo real — mismos resultados. `GET /api/faq` verificado público (sin sesión) con las 12 entradas reales sembradas por la migración 0018. Todos los datos de prueba (2 usuarios, 1 turno, 1 caja, 2 pedidos + pagos, `rate_limit_events`, `login_attempts`) borrados al cerrar — confirmado con `SELECT`/`DELETE ... RETURNING` posterior sin filas restantes. La tabla `faq_entries` sembrada por la migración 0018 se dejó intacta a propósito (es contenido real de producción, no dato de prueba). `/health` en 200 antes y después de cada push.

Commits: `61f8f32` (backend: analytics/sales, analytics/pin-check, faq, migración 0018), `eccbb1d` (frontend: fix `getVoidPin()`) — ambos pusheados a `main`, auto-deploy Railway confirmado.

---

## Auditoría de cierre — FASE 4 (2-sep-2026)

Mismo requisito explícito del dueño del negocio aplicado en el cierre de Fase 1-3: barrido completo de todo lo que Fase 4 tocó (S13-S14 — **S12/SII queda explícitamente excluido de este barrido, ver nota abajo**), en navegador real (Playwright, `channel: 'chrome'` — el Chromium embebido sigue sin funcionar en este macOS 13). Cuenta owner desechable (`qa-s14-fase4close@example.test`, creada por script SQL directo, borrada al cerrar), reutilizada para verificar tanto `cerebro` (Seguridad) como `pos` (Analítica) y una visita anónima a la tienda pública (FAQ).

**Nota explícita sobre S12 (SII/DTE):** S12 no es un pendiente que esta auditoría encontró — es una **decisión de negocio ya tomada y confirmada por el cliente el 1-sep-2026** (ver la fila S12 arriba): la integración de boleta electrónica (Haulmer/OpenFactura/SimpleAPI) se hace explícitamente DESPUÉS de la entrega de v1.0. El sistema opera con **Nota de Venta** (documento no tributario, ver "Adición post-Fase 3 — Nota de Venta + seam DTE") desde antes de que empezara Fase 4, con el seam de `@seul/dte` ya listo para conectar un proveedor real sin rediseño. Esta auditoría de cierre NO reabre esa decisión ni la trata como un hallazgo — la documenta aquí una sola vez, en el resumen de cierre de fase, para que quede explícito y no ambiguo de cara a la entrega.

**Resultado: 3/3 pantallas verificadas cargan y funcionan sin error en navegador real.** No se encontraron bugs nuevos en el barrido de cierre (el único bug de Fase 4 — `getVoidPin()` — ya se encontró y arregló dentro de la propia S14, ver punto 3 arriba).

| Pantalla/flujo | Verificación en esta auditoría | Resultado | Notas |
|---|---|---|---|
| Seguridad (`cerebro/seguridad`, S13 + fix S14) | Playwright real, login owner → navegación directa | ✅ | Carga sin error. "PIN maestro POS" muestra el valor real (`••••` = `1234`, confirmado contra `tienda_config` — el bug de `getVoidPin()` corregido en esta sesión queda verificado en navegador, no solo por curl). Secciones "Solicitudes ARCOP" y "Devoluciones en curso" renderizan (0 pendientes de cada una en este momento — dato real, no de prueba). Cero texto de error tipo "Cannot read properties"/"Application error". |
| Analítica — "Ventas de caja" (`pos`, S14) | Playwright real, login → apertura de turno → apertura de caja → click "Historial de ventas" (`title="Historial de ventas"` en el TopBar) → PIN pad → 4 dígitos del `analytics_pin` real | ✅ | Modal "Acceso a ventas" aparece, PIN correcto desbloquea el drawer "Ventas de caja" (header confirmado: "0 ventas · 0 anuladas" — turno de prueba recién abierto, sin ventas todavía, dato correcto). Cero "PIN incorrecto", cero crash. Único flujo de Analítica que existe hoy en las 4 apps (confirmado por grep en S14 — no hay pantalla "Reportes" en `cerebro`). |
| FAQ pública (`seoulshop.cl/faq`, S14) | Playwright real, visita anónima (sin sesión) | ✅ | Página carga con las 6 categorías (Pedidos, Envíos, Productos, Pagos, Privacidad, Devoluciones) y sus preguntas, acordeón funcional, CTA WhatsApp visible. Cero crash. |

**Errores de consola capturados durante la corrida de Analítica (no relacionados con S14, ya documentados en el cierre de Fase 2):** `net::ERR_CONNECTION_REFUSED` contra `127.0.0.1:9101` — el Print Agent local (puente ESC/POS a la impresora térmica física), no relacionado con ningún endpoint de esta sesión ni con el navegador de prueba (no hay hardware POS conectado a esta máquina), mismo hallazgo ya explicado en la auditoría de cierre de Fase 2.

**Funciones/decisiones de Fase 4 NO cubiertas por esta auditoría (reportado, no bloqueante):**
- **S12 (SII/DTE):** deliberadamente fuera de alcance — ver nota explícita arriba, es una decisión de negocio, no un pendiente técnico de esta auditoría.
- **`POST /api/orders/:id/void`** (anular una venta desde el drawer de Analítica): no se ejercitó el botón "Anular venta" del drawer porque el endpoint no existe (hallazgo documentado en el punto 5 de S14 arriba, fuera de los límites estrictos de esta sesión — dominio checkout/POS). El drawer en sí (lectura del historial + PIN gate) sí quedó completamente verificado.
- **Resolución de solicitudes ARCOP/devoluciones desde el panel:** ya documentado como fuera de alcance en S13 — `seguridad/page.tsx` sigue siendo de solo lectura salvo el PIN maestro.

**Oportunidades de optimización encontradas en Fase 4 (documentadas, no implementadas):**
1. El PIN de analítica (`analytics_pin`) y el PIN maestro de anulación (`void_pin`) son dos secretos de 4 dígitos independientes, ambos gestionados hoy vía el mismo endpoint genérico `PUT /api/tienda-config/:key` — pero solo `void_pin` tiene una UI dedicada para cambiarlo (`PinMaestroSection` en Seguridad). El dueño no tiene hoy una pantalla para cambiar `analytics_pin` sin editar la base de datos directamente o reutilizar el flujo de `ajustes/page.tsx` (que sí lo edita, ver fila "Ajustes" de la sección 6, S06) — están en dos pantallas distintas del panel (Ajustes vs. Seguridad) sin razón de negocio clara para la separación. Vale la pena que una sesión futura de UX (Fase 5/S15) unifique ambos PINs en una sola sección, o al menos los documente lado a lado.
2. La migración 0018 (siembra de `faq_entries`) es "seed si la tabla está vacía", no "seed si nunca corrió" — si el dueño borra manualmente las 12 entradas desde una futura UI de administración de FAQ (que no existe todavía), un redeploy volvería a sembrarlas. No es un bug hoy (no hay forma de borrarlas desde ningún panel todavía) pero vale la pena tenerlo presente si se construye un editor de FAQ en v1.1.
3. Igual que el hallazgo del punto 5 de S14: `POST /api/orders/:id/void` es el gap más visible que queda en el flujo de caja diaria — un cajero puede ver que una venta fue un error pero no puede corregirlo desde el POS, tiene que recurrir a un canal fuera del sistema (WhatsApp/manual). Es el candidato más claro para una sesión de hotfix corta antes de Fase 5, aunque formalmente no es parte del scope de S01-S17.

**Datos de prueba usados en esta auditoría** (1 usuario owner desechable, 1 turno, 1 caja) creados por script SQL directo y borrados al cerrar — verificado con `SELECT` posterior sin filas restantes. `/health` verificado 200 antes, durante (mientras corría Playwright) y después de la auditoría.

---

## FASE 5 — Diseño Premium + Hardening + Entrega (S15-17, 40h)

Investigación de referencia (arquitectura de CRM/POS grandes, 31-ago-2026— ver fuentes al final): API-first con eventos en tiempo real, RBAC que refleja roles reales (no solo autenticación), audit logging con retención por nivel de plan, principio de acceso mínimo en cada endpoint. Todo esto ya quedó incorporado como bloqueadores P0 #2-#4 y como requisito de cada fase — Fase 5 es donde se pule.

**S15 (14h) — Sistema de diseño — ✅ COMPLETA (2-sep-2026):**

Auditoría de las 4 apps contra un solo type ramp/escala de tokens, priorizando las pantallas de mayor uso diario (login, dashboard, POS checkout, sidebar) tal como pedía el alcance de la sesión — pulido visual verificado sin cambiar comportamiento de nada que ya funcionara.

**1. Bug crítico encontrado y arreglado, reportado por el dueño con captura real de producción:** los checkboxes de consentimiento en `/cuenta/registro` (Términos y Política de Privacidad — Ley 21.719) no mostraban ningún cuadro visible. Causa raíz: `--border-editorial` (`packages/tokens/src/hangul.css`) es un shorthand CSS completo (`1px solid rgba(10,10,10,0.12)`), usado incorrectamente en 21 sitios de `apps/web` dentro de `borderColor`/`background` — propiedades que solo aceptan un color puro. CSS inválido en tiempo de valor computado: el navegador descarta la declaración entera y cae al valor inicial (`currentColor`/`transparent`) en vez del gris translúcido diseñado — el mismo bug hacía invisibles dos líneas divisorias de la home. Fix: nuevo token `--color-border-editorial` (el mismo color, sin shorthand) + 21 call-sites migrados, dejando intactos los usos correctos como shorthand completo (`border`/`borderTop`/`borderBottom`/`borderLeft`). Caja del checkbox subida de 16px a 20px con borde de 2px para mejor tamaño táctil en un formulario legal obligatorio. Verificado con Playwright contra producción en dos rondas (antes y después del deploy): marca/desmarca visualmente en ambos checkboxes, screenshots confirmados.

**2. Otro bug sistémico de tokens encontrado:** `--color-text-secondary` se usaba en 12 pantallas (todo el portal B2B, Ajustes, Boleta) pero nunca estuvo definido en ningún archivo de tokens — el navegador heredaba `--color-text` (máxima intensidad) en vez del gris muted esperado, perdiendo jerarquía visual sin que fuera obvio a simple vista. Fix: alias `--color-text-secondary: var(--color-text-muted)` agregado en los 4 bloques de tema (root, media query dark, `[data-theme=dark]`, `[data-theme=light]`).

**3. Auditoría de estilos inline vs. tokens compartidos** (el patrón que el plan señalaba en `sidebar.tsx`): el único inline real de `sidebar.tsx` (avatar del usuario) tenía `color:'#fff'` hardcodeado en vez de `var(--color-text-on-brand)`. `apps/cerebro/login/page.tsx` y `apps/pos/login/page.tsx` convertidos íntegramente de `style={{color:'var(--color-x)'}}` a clases Tailwind ya mapeadas al mismo token (cero cambio visual, mismo valor de token — verificado en Playwright antes/después). **Bug real encontrado en `apps/pos/login/page.tsx`** (no solo estilo): los inputs de correo/contraseña tenían `background:'#ffffff'` y `border:'2px solid #d1d5db'` hardcodeados — gris genérico de Tailwind que ignoraba por completo el sistema de diseño (cream + tokens de borde). Corregido a `bg-surface`/`border-[var(--color-border-strong)]`. Dashboard de cerebro y `checkout-shell.tsx` de POS ya estaban limpios de inline styles significativos — no requirieron cambios. **Resto de estilos inline del proyecto (~80+ archivos, sobre todo Despacho/Ajustes/Seguridad/B2B) documentado como deuda, no migrado** — por la regla explícita de esta sesión de no arriesgar romper pantallas funcionales en una migración cosmética masiva.

**4. Dark mode — contraste y peso tipográfico:** contraste real (fórmula WCAG) de `--color-text-muted` auditado en los 3 bloques de tema — el toggle manual de POS (`[data-theme="dark"]`) daba 4.49:1 contra `--color-surface-elevated`, bajo el mínimo AA de 4.5:1 para texto normal. Subido de `#888888` a `#949494` (~5.25:1 contra superficie elevada, ~6.3:1 contra fondo base, sin cambio de tono perceptible). Los otros 2 bloques (media query automática del sistema, usada por cerebro y apps/web) ya cumplían AA con margen amplio (8.7:1) — sin cambios. Auditados todos los usos de `font-bold` en elementos `<p>` (54 instancias, grep completo en las 4 apps): 100% son montos/números/etiquetas cortas o títulos de 1-3 palabras, ninguno es texto de párrafo largo — **no se encontró el patrón literal que describe este punto del plan** (bold en texto largo); no se tocó nada por no haber nada que corregir.

**5. Estados de error consistentes:** nuevo componente `ErrorState` en `@seul/ui` (`packages/ui/src/error-state.tsx`, exportado del barrel) — copy fijo en español natural, nunca expone `error.message`. Nuevo helper `friendlyErrorMessage()` (`packages/ui/src/lib/utils.ts`) — filtra patrones de excepción JS sin traducir ("Cannot read propert...", "is not a function", "TypeError", etc.) dejando pasar intactos los mensajes de negocio ya existentes ("No hay turno activo. Recarga el POS.", errores devueltos por la API). Aplicado en `apps/cerebro/(admin)/error.tsx` y `apps/web/(shop)/error.tsx` (ambos renderizaban `error.message` crudo — el bug literal del plan, "Cannot read properties of undefined" podía llegar a pantalla), `apps/pos/checkout-shell.tsx` (el catch de `handleConfirm` mostraba `err.message` crudo al cajero, la pantalla de mayor riesgo del sistema; los mensajes de negocio existentes se preservan exactamente igual — verificado leyendo cada mensaje que ese catch puede recibir), y `apps/cerebro/despacho/page.tsx` + `usuarios/page.tsx`. **Hallazgo: `apps/pos` y `apps/repartidor` no tenían NINGÚN error boundary** — un render roto mostraba la pantalla genérica sin estilo de Next.js directamente sobre la caja registradora o la PWA del repartidor. Creados `apps/pos/src/app/error.tsx` y `apps/repartidor/src/app/error.tsx`. `apps/web/(b2b)/error.tsx` ya tenía copy correcto (sin `error.message` crudo), solo usa hex hardcodeados en vez de tokens — **documentado como deuda, no tocado** (bajo tráfico/bajo riesgo).

**6. Copy engañoso corregido** (deuda documentada en el cierre de Fase 1, sección 6, punto 3 de optimizaciones): tooltip "Eliminar usuario" en `usuarios/page.tsx` — el botón en realidad hace un soft-delete (`is_active=false`), no un borrado real. Ahora dice "Eliminar usuario (desactiva la cuenta, no borra su historial)".

**7. EmptyState:** confirmado que solo se usaba en 2 pantallas (Dashboard, POS) pese a estar documentado en `CLAUDE.md` como el patrón compartido. Extendido con overrides opcionales `title`/`description` (retrocompatible, cero cambio en los 2 usos existentes) y adoptado en `despacho/page.tsx` (reemplaza un `<div>` de texto plano). El resto de estados vacíos custom (~10 pantallas) **documentado como deuda** — mismo criterio de riesgo/prioridad que el punto 3.

**8. Banners de error con colores Tailwind literales (no tokens, no responden a dark mode):** encontrados en 16 archivos (`bg-red-50`/`text-red-700`/etc.). Corregidos los 2 de mayor uso diario (`despacho/page.tsx`, `usuarios/page.tsx`) a `bg-error-subtle`/`text-error`. **Los 14 restantes documentados como deuda:** `turnos/page.tsx`, `ajustes/page.tsx`, `b2b/solicitudes/page.tsx`, `clientes/[id]/page.tsx`, `inventory-lot-modal.tsx`, `void-auth-modal.tsx` (POS), y 6 pantallas del portal B2B + `checkout/page.tsx` en `apps/web`.

**Hallazgo adicional sin corregir, documentado para decisión del dueño:** `apps/web` en su capa editorial/K-wave (tokens `--color-baek`/`--color-heuk`/`--border-editorial`) no responde a `prefers-color-scheme: dark` en absoluto — solo los tokens "semánticos" (`--color-background`, `--color-text`, etc., usados por cerebro y el resto de apps/web) tienen bloque de dark mode. No está claro si es una decisión de diseño deliberada (estética editorial tipo catálogo impreso, siempre clara) o un gap — queda para que el dueño confirme la intención antes de tocarlo.

**Verificación:** `tsc --noEmit` y `next build` limpios en las 4 apps antes de cada commit. Playwright (`channel: 'chrome'`) contra producción en dos rondas (antes y después del deploy) con cuenta desechable `qa-test-s15-visual@example.test` creada por SQL directo — **nota encontrada al crearla:** `packages/db/src/seed-owner.ts` genera hashes con un formato (base64, 210k iteraciones) incompatible con el verificador real de `packages/api/src/services/password.service.ts` (hex, 100k iteraciones, prefijo `$pbkdf2$`) — ese script generaría cuentas que **nunca podrían iniciar sesión**; no se usó, se generó el hash a mano con el formato correcto. Deuda/bug fuera de alcance de S15, alguien debería corregir o eliminar ese script. Cuenta + su sesión borradas al cerrar (verificado con `SELECT` posterior). Cubierto con Playwright: login (cerebro, POS) y registro (checkboxes) en su estado real de producción antes y después del fix; dashboard + sidebar de cerebro tras login real (avatar del sidebar renderiza correctamente); home y catálogo de `apps/web`. `vercel inspect --logs` limpio en los 4 proyectos (cerebro, pos, web, repartidor) tras el push, los 4 en estado `Ready`. `curl`: API `/health` 200, `apps/web` `/health` 200, `cmr.seoulshop.cl/login` 200, `pos.seoulshop.cl/login` 200, `drive.seoulshop.cl/` 200 (su ruta `/health` da 307→`/`, comportamiento de middleware preexistente no tocado por esta sesión).

Commits: `a3f68e7` (tokens compartidos + `@seul/ui`), `e8294cb` (fix checkboxes/dividers `apps/web`), `d2c4614` (ErrorState en `(shop)/error.tsx`), `858b16d` (`apps/cerebro`), `0eae857` (`apps/pos`), `be21590` (`apps/repartidor`) — los 6 pusheados a `main`, auto-deploy Vercel confirmado en las 4 apps. Railway sin redeploy de código nuevo (`packages/api` no fue tocado esta sesión).

**No se avanzó a S16.**

**S16 (14h) — Hardening:**
- Audit log real (tabla + UI) para acciones administrativas sensibles (crear/desactivar usuario, resetear contraseña, editar precio) — hoy no existe ninguno.
- Test de carga básico (repetir el patrón k6 que ya se usó en el CRM de VÉRTICE) sobre `/api/auth/login` y los endpoints de POS bajo uso concurrente (varias cajas abriendo turno a la vez).
- Revisar que ningún endpoint nuevo de Fase 1-4 quedó sin `requireSession()` / validación de rol.

**S17 (12h) — Entrega:**
- Actualizar `SEUL_KING_OS_v1.0_MANUAL_CLIENTE.md` y `SEUL_KING_OS_v1_Manual.html` con el estado final real (quitar todos los "próximamente"/"pendiente" que Fase 1-4 ya resolvieron).
- Checklist de entrega: las 4 apps + portal B2B + tienda cliente funcionando en navegador real, sesión de capacitación con Jorge y Mario (ya contemplada en el manual), SLA de 30 días activo.
- **Gate final:** cero pantalla con "Error al cargar", cero endpoint 404 en las rutas que el frontend llama — verificado con el mismo método de auditoría de la sección 7, corriéndolo de nuevo y confirmando 0 faltantes.

---

## Fuentes (investigación de arquitectura y diseño, 31-ago-2026)

- [Enterprise CRM Security Framework: Comprehensive Protection Strategies](https://www.stacksync.com/blog/enterprise-crm-security-framework-comprehensive-protection-strategies-for-2025)
- [Security Best Practices for Real-Time CRM Data Integration](https://www.stacksync.com/blog/security-best-practices-for-real-time-crm-data-integration)
- [Role-Based Access Control Best Practices for 2026](https://www.techprescient.com/blogs/role-based-access-control-best-practices/)
- [Syncally Goes Enterprise: SSO, Audit Logs, RBAC, and SOC 2](https://www.syncally.app/blog/syncally-enterprise-launch-sso-audit-logs-rbac)
- [Retail Analytics Dashboard Development and Integration Guide](https://rbmsoft.com/blogs/retail-analytics-dashboard-development-and-integration/)
- [Dark Mode Design: UX Best Practices, Accessibility Rules](https://medium.com/@focalin.web/dark-mode-design-ux-best-practices-accessibility-rules-how-to-implement-it-right-f1153df0f48e)
- [The 7 Biggest Typography Mistakes Designers Make When Switching from Light to Dark Mode](https://medium.com/@dollyborade07/the-7-biggest-typography-mistakes-designers-make-when-switching-from-light-to-dark-mode-0472f542763d)
