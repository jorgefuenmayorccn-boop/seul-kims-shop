# El Cerebro — Next.js env
# Copiar como .env.local y completar valores

# URL del worker API (Hono en Cloudflare Workers)
NEXT_PUBLIC_API_URL=http://localhost:8787

# Neon PostgreSQL (mismo string que packages/api/.dev.vars)
DATABASE_URL=postgresql://...
